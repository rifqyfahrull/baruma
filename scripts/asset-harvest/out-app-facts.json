[
 {
  "id": "action:updateRoom",
  "kind": "action",
  "name": "updateRoom",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"updateRoom\"), roomId: z.string().min(1), patch: roomPatchSchema }),\n\n// dokumentasi prompt\n'- {\"type\":\"updateRoom\",\"roomId\":\"<id>\",\"patch\":{...}} — patch boleh: name(string), type(RoomType), floorId(id lantai tujuan untuk pindah lantai), zoneId(id zona open-plan; samakan beberapa ruang untuk satu area terbuka), levelOffsetM(meter, split-level; +naik/-turun), x, y, width, depth (meter), locked(bool), requiresNaturalLight(bool), requiresVentilation(bool), stairDirection(\"n|s|w|e\", ruang tipe tangga: arah NAIK), railingStyle(\"kaca|besi|tembok|kayu\", balkon: model railing sisi terbuka), railingModelUrl/railingModelAssetId(model railing GLB dari library; null = lepas, kembali ke railingStyle).\\n' +\n\n// penerapan sebenarnya (apply.ts)\nif (a.type === \"updateRoom\" || a.type === \"addOpening\" || a.type === \"deleteRoom\") {\r\n        if (!store.layout?.rooms.some((r) => r.id === a.roomId)) continue\r\n        if (a.type === \"updateRoom\") store.updateRoom(a.roomId, a.patch)\r\n        else if (a.type === \"addOpening\") {\r\n          // Optional specific kind (e.g. garage_door): the store only creates\r\n          // hinged_door/sliding_window, so create first, then re-kind the new\r\n          // opening with the kind's full defaults (2.7×2.2 gara\n…\nif (a.type === \"updateRoom\") store.updateRoom(a.roomId, a.patch)\r\n        else if (a.type === \"addOpening\") {\r\n          // Optional specific kind (e.g. garage_door): the store only creates\r\n          // hinged_door/sliding_window, so create first, then re-kind the new\r\n          // opening with the kind's full defaults (2.7×2.2 garage door, etc.).\r\n          const kindDefaults = a.kind ? openingDefaultsForKind(a.kind) : null\r\n          const before = new Set(store.layout.openings.map((o) => o.id))\n\n// editor-store.ts — updateRoom\nupdateRoom: (id, patch) =>\n      commit((l) => {\n        const room = l.rooms.find((r) => r.id === id);\n        if (!room) return;\n        Object.assign(room, patch);\n        if (patch.width !== undefined || patch.depth !== undefined) {\n          room.areaM2 = roomArea(room.width, room.depth);\n        }\n        // Moving a room to another floor takes its openings along.\n        if (patch.floorId !== undefined) {\n          for (const op of l.openings) {\n            if (op.wallId.startsWith(`${id}:`)) op.floorId = patch.floorId;\n          }\n        }\n      }),\n\n    toggleLock: (id) =>\n      commit((l) => {\n        const room = l.rooms.find((r) => r.id === id);\n        if (room) room.locked = !room.locked;\n      }),\n\n    deleteSelected: () => {\n      const { selectedObjectId } = get();\n      if (selectedObjectId) get().deleteObject(selectedObjectId);\n    },\n\n    deleteObject: (id) => {"
 },
 {
  "id": "action:addOpening",
  "kind": "action",
  "name": "addOpening",
  "evidence": "// skema (actions.ts)\nz.object({\r\n    type: z.literal(\"addOpening\"),\r\n    roomId: z.string().min(1),\r\n    side: sideSchema,\r\n    positionM: z.number().finite().nonnegative(),\r\n    openingType: z.enum([\"door\", \"window\"]),\r\n    /** Jenis spesifik opsional (mis. \"garage_door\" 2.7×2.2) — dipasang setelah\r\n     *  bukaan dibuat; openingType diturunkan dari kind bila keduanya bentrok. */\r\n    kind: openingKindSchema.optional(),\r\n  }),\n\n// dokumentasi prompt\n'- {\"type\":\"addOpening\",\"roomId\":\"<id>\",\"side\":\"n|e|s|w\",\"positionM\":<meter dari sudut>,\"openingType\":\"door|window\",\"kind?\":\"<OpeningKind>\"} — kind opsional untuk jenis spesifik, mis. \"garage_door\" = pintu garasi sectional (default 2.7×2.2 m, cocok di carport).\\n' +\n\n// penerapan sebenarnya (apply.ts)\nif (a.type === \"updateRoom\" || a.type === \"addOpening\" || a.type === \"deleteRoom\") {\r\n        if (!store.layout?.rooms.some((r) => r.id === a.roomId)) continue\r\n        if (a.type === \"updateRoom\") store.updateRoom(a.roomId, a.patch)\r\n        else if (a.type === \"addOpening\") {\r\n          // Optional specific kind (e.g. garage_door): the store only creates\r\n          // hinged_door/sliding_window, so create first, then re-kind the new\r\n          // opening with the kind's full defaults (2.7×2.2 garage door, etc.).\n…\nelse if (a.type === \"addOpening\") {\r\n          // Optional specific kind (e.g. garage_door): the store only creates\r\n          // hinged_door/sliding_window, so create first, then re-kind the new\r\n          // opening with the kind's full defaults (2.7×2.2 garage door, etc.).\r\n          const kindDefaults = a.kind ? openingDefaultsForKind(a.kind) : null\r\n          const before = new Set(store.layout.openings.map((o) => o.id))\r\n          store.addOpening(a.roomId, a.side, a.positionM, kindDefaults?.type ?\n\n// editor-store.ts — updateRoom\nupdateRoom: (id, patch) =>\n      commit((l) => {\n        const room = l.rooms.find((r) => r.id === id);\n        if (!room) return;\n        Object.assign(room, patch);\n        if (patch.width !== undefined || patch.depth !== undefined) {\n          room.areaM2 = roomArea(room.width, room.depth);\n        }\n        // Moving a room to another floor takes its openings along.\n        if (patch.floorId !== undefined) {\n          for (const op of l.openings) {\n            if (op.wallId.startsWith(`${id}:`)) op.floorId = patch.floorId;\n          }\n        }\n      }),\n\n    toggleLock: (id) =>\n      commit((l) => {\n        const room = l.rooms.find((r) => r.id === id);\n        if (room) room.locked = !room.locked;\n      }),\n\n    deleteSelected: () => {\n      const { selectedObjectId } = get();\n      if (selectedObjectId) get().deleteObject(selectedObjectId);\n    },\n\n    deleteObject: (id) => {\n\n// editor-store.ts — addOpening\naddOpening: (roomId, side, positionM, type) =>\n      commit((l) => {\n        const room = l.rooms.find((r) => r.id === roomId);\n        if (!room) return;\n        const kind: OpeningKind =\n          type === \"door\" ? \"hinged_door\" : \"sliding_window\";\n        l.openings.push({\n          id: `op-${Math.abs(hashStr(roomId + side + positionM + l.openings.length))}`,\n          floorId: room.floorId,\n          wallId: openingWallId(roomId, side),\n          ...openingDefaultsForKind(kind),\n          positionM: round2(positionM),\n        });\n      }),\n\n    updateOpening: (id, patch) =>\n      commit((l) => {\n        const op = l.openings.find((o) => o.id === id);\n        if (op) Object.assign(op, patch);\n      }),\n\n    setRoof: (patch) =>\n      commit((l) => {\n        l.roof = { ...DEFAULT_ROOF, ...l.roof, ...patch };\n      }),\n\n    addRoofZone: (zone) => {\n      commit((l) => {\n        l.roo"
 },
 {
  "id": "action:deleteRoom",
  "kind": "action",
  "name": "deleteRoom",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"deleteRoom\"), roomId: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"deleteRoom\",\"roomId\":\"<id>\"}.\\n' +\n\n// penerapan sebenarnya (apply.ts)\nif (a.type === \"updateRoom\" || a.type === \"addOpening\" || a.type === \"deleteRoom\") {\r\n        if (!store.layout?.rooms.some((r) => r.id === a.roomId)) continue\r\n        if (a.type === \"updateRoom\") store.updateRoom(a.roomId, a.patch)\r\n        else if (a.type === \"addOpening\") {\r\n          // Optional specific kind (e.g. garage_door): the store only creates\r\n          // hinged_door/sliding_window, so create first, then re-kind the new\r\n          // opening with the kind's full defaults (2.7×2.2 garage door, etc.).\r\n          const kindDefaults = a.kind\n\n// editor-store.ts — updateRoom\nupdateRoom: (id, patch) =>\n      commit((l) => {\n        const room = l.rooms.find((r) => r.id === id);\n        if (!room) return;\n        Object.assign(room, patch);\n        if (patch.width !== undefined || patch.depth !== undefined) {\n          room.areaM2 = roomArea(room.width, room.depth);\n        }\n        // Moving a room to another floor takes its openings along.\n        if (patch.floorId !== undefined) {\n          for (const op of l.openings) {\n            if (op.wallId.startsWith(`${id}:`)) op.floorId = patch.floorId;\n          }\n        }\n      }),\n\n    toggleLock: (id) =>\n      commit((l) => {\n        const room = l.rooms.find((r) => r.id === id);\n        if (room) room.locked = !room.locked;\n      }),\n\n    deleteSelected: () => {\n      const { selectedObjectId } = get();\n      if (selectedObjectId) get().deleteObject(selectedObjectId);\n    },\n\n    deleteObject: (id) => {"
 },
 {
  "id": "action:addRoom",
  "kind": "action",
  "name": "addRoom",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"addRoom\"), roomType: roomTypeSchema, x: z.number().finite().optional(), y: z.number().finite().optional() }),\n\n// dokumentasi prompt\n'- {\"type\":\"addRoom\",\"roomType\":\"<RoomType>\",\"x\":<meter?>,\"y\":<meter?>} — buat ruang baru di lantai aktif.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"addRoom\") {\r\n        store.addRoom(a.roomType, a.x ?? 0, a.y ?? 0)\r\n      } else if (a.type === \"addFloor\") {\r\n        store.addFloor()\r\n      } else if (a.type === \"removeFloor\") {\r\n        store.removeFloor(a.floorId)\r\n      } else if (a.type === \"updateOpening\") {\r\n        const patch: Partial<Opening> = {}\r\n        // `kind` is the source of truth: it resets metadata + dimensions to the\r\n        // kind's defaults (QuickEditor pattern); explicit fields below win.\r\n        if (a.p\n\n// editor-store.ts — addRoom\naddRoom: (type, x, y) => {\n      const { selectedFloorId, site, snapEnabled, gridSize } = get();\n      if (!selectedFloorId || !site) return;\n      let newId = \"\";\n      commit((l) => {\n        const room = makeRoom(\n          type,\n          x,\n          y,\n          selectedFloorId,\n          site,\n          snapEnabled ? (v) => snap(v, gridSize) : undefined,\n        );\n        newId = room.id;\n        l.rooms.push(room);\n      });\n      set({ selectedObjectId: newId, pendingRoomType: null });\n    },\n\n    addFloor: () => {\n      const { layout } = get();\n      if (!layout) return;\n      const floor = makeFloor(layout.floors);\n      commit((l) => {\n        const rooftopIdx = l.floors.findIndex((f) => f.id === \"floor-rooftop\");\n        if (rooftopIdx >= 0) l.floors.splice(rooftopIdx, 0, floor);\n        else l.floors.push(floor);\n      });\n      set({ selectedFloorId: floor.id, select\n\n// editor-store.ts — addFloor\naddFloor: () => {\n      const { layout } = get();\n      if (!layout) return;\n      const floor = makeFloor(layout.floors);\n      commit((l) => {\n        const rooftopIdx = l.floors.findIndex((f) => f.id === \"floor-rooftop\");\n        if (rooftopIdx >= 0) l.floors.splice(rooftopIdx, 0, floor);\n        else l.floors.push(floor);\n      });\n      set({ selectedFloorId: floor.id, selectedObjectId: null });\n    },\n\n    setRooftop: (enabled) => {\n      const { layout } = get();\n      if (!layout) return;\n      const has = layout.floors.some((f) => f.id === \"floor-rooftop\");\n      if (enabled === has) return;\n      commit((l) => {\n        if (enabled) {\n          l.floors.push({\n            id: \"floor-rooftop\",\n            level: l.floors.length + 1,\n            name: \"Rooftop\",\n            heightM: 0.3, // slab tipis — rooftop = dak terbuka (paritas generator)\n          });\n        } else {"
 },
 {
  "id": "action:addFloor",
  "kind": "action",
  "name": "addFloor",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"addFloor\") }),\n\n// dokumentasi prompt\n'- {\"type\":\"addFloor\"} — tambah lantai baru (kosong).\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"addFloor\") {\r\n        store.addFloor()\r\n      } else if (a.type === \"removeFloor\") {\r\n        store.removeFloor(a.floorId)\r\n      } else if (a.type === \"updateOpening\") {\r\n        const patch: Partial<Opening> = {}\r\n        // `kind` is the source of truth: it resets metadata + dimensions to the\r\n        // kind's defaults (QuickEditor pattern); explicit fields below win.\r\n        if (a.patch.kind != null) Object.assign(patch, openingDefaultsForKind(a.patch.kind))\r\n        else if (a\n\n// editor-store.ts — addFloor\naddFloor: () => {\n      const { layout } = get();\n      if (!layout) return;\n      const floor = makeFloor(layout.floors);\n      commit((l) => {\n        const rooftopIdx = l.floors.findIndex((f) => f.id === \"floor-rooftop\");\n        if (rooftopIdx >= 0) l.floors.splice(rooftopIdx, 0, floor);\n        else l.floors.push(floor);\n      });\n      set({ selectedFloorId: floor.id, selectedObjectId: null });\n    },\n\n    setRooftop: (enabled) => {\n      const { layout } = get();\n      if (!layout) return;\n      const has = layout.floors.some((f) => f.id === \"floor-rooftop\");\n      if (enabled === has) return;\n      commit((l) => {\n        if (enabled) {\n          l.floors.push({\n            id: \"floor-rooftop\",\n            level: l.floors.length + 1,\n            name: \"Rooftop\",\n            heightM: 0.3, // slab tipis — rooftop = dak terbuka (paritas generator)\n          });\n        } else {\n\n// editor-store.ts — removeFloor\nremoveFloor: (floorId) => {\n      const { layout } = get();\n      if (!layout || layout.floors.length <= 1) return;\n      commit((l) => {\n        l.floors = l.floors.filter((f) => f.id !== floorId);\n        l.rooms = l.rooms.filter((r) => r.floorId !== floorId);\n        l.openings = l.openings.filter((o) => o.floorId !== floorId);\n        if (l.exteriorElements) {\n          const next = l.exteriorElements.filter(\n            (el) => el.floorId !== floorId,\n          );\n          if (next.length === 0) delete l.exteriorElements;\n          else l.exteriorElements = next as ExteriorElement[];\n        }\n        if (l.roofZones) {\n          l.roofZones = l.roofZones.filter((z) => z.floorId !== floorId);\n          if (l.roofZones.length === 0) delete l.roofZones;\n        }\n      });\n      set((s) => ({\n        selectedFloorId:\n          s.selectedFloorId === floorId\n            ? (get().la"
 },
 {
  "id": "action:removeFloor",
  "kind": "action",
  "name": "removeFloor",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"removeFloor\"), floorId: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"removeFloor\",\"floorId\":\"<id lantai>\"} — hapus lantai + isinya.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"removeFloor\") {\r\n        store.removeFloor(a.floorId)\r\n      } else if (a.type === \"updateOpening\") {\r\n        const patch: Partial<Opening> = {}\r\n        // `kind` is the source of truth: it resets metadata + dimensions to the\r\n        // kind's defaults (QuickEditor pattern); explicit fields below win.\r\n        if (a.patch.kind != null) Object.assign(patch, openingDefaultsForKind(a.patch.kind))\r\n        else if (a.patch.openingType != null) patch.type = a.patch.openingType\n\n// editor-store.ts — removeFloor\nremoveFloor: (floorId) => {\n      const { layout } = get();\n      if (!layout || layout.floors.length <= 1) return;\n      commit((l) => {\n        l.floors = l.floors.filter((f) => f.id !== floorId);\n        l.rooms = l.rooms.filter((r) => r.floorId !== floorId);\n        l.openings = l.openings.filter((o) => o.floorId !== floorId);\n        if (l.exteriorElements) {\n          const next = l.exteriorElements.filter(\n            (el) => el.floorId !== floorId,\n          );\n          if (next.length === 0) delete l.exteriorElements;\n          else l.exteriorElements = next as ExteriorElement[];\n        }\n        if (l.roofZones) {\n          l.roofZones = l.roofZones.filter((z) => z.floorId !== floorId);\n          if (l.roofZones.length === 0) delete l.roofZones;\n        }\n      });\n      set((s) => ({\n        selectedFloorId:\n          s.selectedFloorId === floorId\n            ? (get().la"
 },
 {
  "id": "action:updateOpening",
  "kind": "action",
  "name": "updateOpening",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"updateOpening\"), openingId: z.string().min(1), patch: openingPatchSchema }),\n\n// dokumentasi prompt\n'- {\"type\":\"updateOpening\",\"openingId\":\"<id>\",\"patch\":{positionM?,widthM?,heightM?,openingType?,kind?,frameColor?,sillHeightM?,headHeightM?,modelUrl?,modelAssetId?}} — kind = jenis spesifik (lihat OpeningKind; mengubah kind me-reset dimensi/metadata ke default jenis itu, field lain di patch yang sama menimpanya); frameColor = warna kusen custom \"#rrggbb\"; modelUrl/modelAssetId = daun/panel GLB kustom dari library (null = lepas).\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"updateOpening\") {\r\n        const patch: Partial<Opening> = {}\r\n        // `kind` is the source of truth: it resets metadata + dimensions to the\r\n        // kind's defaults (QuickEditor pattern); explicit fields below win.\r\n        if (a.patch.kind != null) Object.assign(patch, openingDefaultsForKind(a.patch.kind))\r\n        else if (a.patch.openingType != null) patch.type = a.patch.openingType\r\n        if (a.patch.positionM != null) patch.positionM = a.patch.positionM\r\n        if (a.p"
 },
 {
  "id": "action:deleteOpening",
  "kind": "action",
  "name": "deleteOpening",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"deleteOpening\"), openingId: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"deleteOpening\",\"openingId\":\"<id>\"}.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"deleteOpening\") {\r\n        store.deleteObject(a.openingId)\r\n      } else if (a.type === \"setRoof\") {\r\n        const { fascia, ...rest } = a.patch\r\n        const patch: Partial<RoofSpec> = { ...rest }\r\n        // fascia:null = turn the band OFF. The store merges {...l.roof, ...patch},\r\n        // so send the key EXPLICITLY as undefined to actually delete it (a key\r\n        // absent from the patch would leave the old fascia in place).\r\n        if (fascia !== undefined) patch.fascia =\n\n// editor-store.ts — deleteObject\ndeleteObject: (id) => {\n      const { layout } = get();\n      if (!layout) return;\n      const isRoom = layout.rooms.some((r) => r.id === id);\n      const isExterior = layout.exteriorElements?.some((e) => e.id === id);\n      const isRoofZone = layout.roofZones?.some((z) => z.id === id);\n      if (isExterior) {\n        get().removeExteriorElement(id);\n        if (get().selectedObjectId === id) set({ selectedObjectId: null });\n        return;\n      }\n      if (isRoofZone) {\n        get().removeRoofZone(id);\n        if (get().selectedObjectId === id) set({ selectedObjectId: null });\n        return;\n      }\n      commit((l) => {\n        if (isRoom) {\n          l.rooms = l.rooms.filter((r) => r.id !== id);\n          l.openings = l.openings.filter((o) => !o.wallId.startsWith(`${id}:`));\n          // Elemen & cladding fasad menempel di dinding ruang ini → ikut hilang.\n          if (l.facade"
 },
 {
  "id": "action:setRoof",
  "kind": "action",
  "name": "setRoof",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"setRoof\"), patch: roofPatchSchema }),\n\n// dokumentasi prompt\n'- {\"type\":\"setRoof\",\"patch\":{type?:\"datar|pelana|limasan|miring\",slopeDeg?:<15-40; khusus type miring boleh 5-40>,overhangM?:<0-1 meter>,material?:\"genteng_beton|genteng_keramik|metal|aspal\",lowSide?:\"n|s|w|e\",fascia?:{heightM:<0.1-0.8>,color:\"#rrggbb\"}|null}} — ubah parameter atap (semua field opsional, hanya kirim yang berubah). \"miring\" = atap skillion satu bidang; lowSide = sisi RENDAH (arah air mengalir), hanya untuk type miring. fascia = lis band gelap tepi atap datar/dak/balkon (gaya modern); fascia:null = MATIKAN lis; jangan kirim fascia bila tidak diubah.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"setRoof\") {\r\n        const { fascia, ...rest } = a.patch\r\n        const patch: Partial<RoofSpec> = { ...rest }\r\n        // fascia:null = turn the band OFF. The store merges {...l.roof, ...patch},\r\n        // so send the key EXPLICITLY as undefined to actually delete it (a key\r\n        // absent from the patch would leave the old fascia in place).\r\n        if (fascia !== undefined) patch.fascia = fascia ?? undefined\r\n        store.setRoof(patch)\r\n      } else if (a.type === \"addRoofZo\n\n// editor-store.ts — setRoof\nsetRoof: (patch) =>\n      commit((l) => {\n        l.roof = { ...DEFAULT_ROOF, ...l.roof, ...patch };\n      }),\n\n    addRoofZone: (zone) => {\n      commit((l) => {\n        l.roofZones = [...(l.roofZones ?? []), { ...zone }];\n      });\n      set({ selectedObjectId: zone.id, pendingRoofZoneType: null });\n    },\n\n    updateRoofZone: (id, patch) =>\n      commit((l) => {\n        const zone = l.roofZones?.find((z) => z.id === id);\n        if (!zone) return;\n        Object.assign(zone, patch);\n        zone.x = round2(zone.x);\n        zone.y = round2(zone.y);\n        zone.widthM = round2(Math.max(MIN_ROOF_ZONE, zone.widthM));\n        zone.depthM = round2(Math.max(MIN_ROOF_ZONE, zone.depthM));\n        zone.slopeDeg = round2(Math.max(0, Math.min(60, zone.slopeDeg)));\n        zone.overhangM = round2(Math.max(0, Math.min(2, zone.overhangM)));\n        if (zone.type !== \"miring\") delete zone.lowSid"
 },
 {
  "id": "action:addRoofZone",
  "kind": "action",
  "name": "addRoofZone",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"addRoofZone\"), zone: roofZoneDraftSchema }),\n\n// dokumentasi prompt\n'- {\"type\":\"addRoofZone\",\"zone\":{type:\"datar|pelana|limasan|miring\",x:<center m>,y:<center m>,widthM:<m>,depthM:<m>,slopeDeg?:<0-60>,overhangM?:<0-1>,materialId?:\"genteng_beton|genteng_keramik|metal|aspal\",lowSide?:\"n|s|w|e\",floorId?:\"<id lantai>\"}} — tambah zona atap eksplisit. Gunakan saat user meminta atap kombinasi/split/massa berbeda; koordinat x/y adalah titik tengah zona.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"addRoofZone\") {\r\n        const zone: RoofZone = {\r\n          id: `roofz-ai-${nanoid(6)}`,\r\n          slopeDeg: a.zone.type === \"datar\" ? 0 : 30,\r\n          overhangM: 0.2,\r\n          materialId: \"genteng_beton\",\r\n          ...a.zone,\r\n        }\r\n        store.addRoofZone(zone)\r\n      } else if (a.type === \"updateRoofZone\") {\r\n        if (!store.layout?.roofZones?.some((z) => z.id === a.id)) continue\r\n        store.updateRoofZone(a.id, a.patch)\r\n      } else if (a.type === \"removeRoof\n\n// editor-store.ts — addRoofZone\naddRoofZone: (zone) => {\n      commit((l) => {\n        l.roofZones = [...(l.roofZones ?? []), { ...zone }];\n      });\n      set({ selectedObjectId: zone.id, pendingRoofZoneType: null });\n    },\n\n    updateRoofZone: (id, patch) =>\n      commit((l) => {\n        const zone = l.roofZones?.find((z) => z.id === id);\n        if (!zone) return;\n        Object.assign(zone, patch);\n        zone.x = round2(zone.x);\n        zone.y = round2(zone.y);\n        zone.widthM = round2(Math.max(MIN_ROOF_ZONE, zone.widthM));\n        zone.depthM = round2(Math.max(MIN_ROOF_ZONE, zone.depthM));\n        zone.slopeDeg = round2(Math.max(0, Math.min(60, zone.slopeDeg)));\n        zone.overhangM = round2(Math.max(0, Math.min(2, zone.overhangM)));\n        if (zone.type !== \"miring\") delete zone.lowSide;\n        if (zone.type === \"datar\") zone.slopeDeg = 0;\n      }),\n\n    removeRoofZone: (id) =>\n      commit((l) =>\n\n// editor-store.ts — updateRoofZone\nupdateRoofZone: (id, patch) =>\n      commit((l) => {\n        const zone = l.roofZones?.find((z) => z.id === id);\n        if (!zone) return;\n        Object.assign(zone, patch);\n        zone.x = round2(zone.x);\n        zone.y = round2(zone.y);\n        zone.widthM = round2(Math.max(MIN_ROOF_ZONE, zone.widthM));\n        zone.depthM = round2(Math.max(MIN_ROOF_ZONE, zone.depthM));\n        zone.slopeDeg = round2(Math.max(0, Math.min(60, zone.slopeDeg)));\n        zone.overhangM = round2(Math.max(0, Math.min(2, zone.overhangM)));\n        if (zone.type !== \"miring\") delete zone.lowSide;\n        if (zone.type === \"datar\") zone.slopeDeg = 0;\n      }),\n\n    removeRoofZone: (id) =>\n      commit((l) => {\n        if (!l.roofZones) return;\n        l.roofZones = l.roofZones.filter((z) => z.id !== id);\n        if (l.roofZones.length === 0) delete l.roofZones;\n      }),\n\n    splitRoofZone: (id, axis) =>"
 },
 {
  "id": "action:updateRoofZone",
  "kind": "action",
  "name": "updateRoofZone",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"updateRoofZone\"), id: z.string().min(1), patch: roofZonePatchSchema }),\n\n// dokumentasi prompt\n'- {\"type\":\"updateRoofZone\",\"id\":\"<id zona>\",\"patch\":{type?,x?,y?,widthM?,depthM?,slopeDeg?,overhangM?,materialId?,lowSide?,floorId?}} — ubah zona atap eksplisit yang ada di DENAH.roofZones.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"updateRoofZone\") {\r\n        if (!store.layout?.roofZones?.some((z) => z.id === a.id)) continue\r\n        store.updateRoofZone(a.id, a.patch)\r\n      } else if (a.type === \"removeRoofZone\") {\r\n        if (!store.layout?.roofZones?.some((z) => z.id === a.id)) continue\r\n        store.removeRoofZone(a.id)\r\n      } else if (a.type === \"setRooftop\") {\r\n        if (!store.layout) continue\r\n        store.setRooftop(a.enabled)\r\n      } else if (a.type === \"setWallCladding\") {\r\n        const par\n\n// editor-store.ts — updateRoofZone\nupdateRoofZone: (id, patch) =>\n      commit((l) => {\n        const zone = l.roofZones?.find((z) => z.id === id);\n        if (!zone) return;\n        Object.assign(zone, patch);\n        zone.x = round2(zone.x);\n        zone.y = round2(zone.y);\n        zone.widthM = round2(Math.max(MIN_ROOF_ZONE, zone.widthM));\n        zone.depthM = round2(Math.max(MIN_ROOF_ZONE, zone.depthM));\n        zone.slopeDeg = round2(Math.max(0, Math.min(60, zone.slopeDeg)));\n        zone.overhangM = round2(Math.max(0, Math.min(2, zone.overhangM)));\n        if (zone.type !== \"miring\") delete zone.lowSide;\n        if (zone.type === \"datar\") zone.slopeDeg = 0;\n      }),\n\n    removeRoofZone: (id) =>\n      commit((l) => {\n        if (!l.roofZones) return;\n        l.roofZones = l.roofZones.filter((z) => z.id !== id);\n        if (l.roofZones.length === 0) delete l.roofZones;\n      }),\n\n    splitRoofZone: (id, axis) =>\n\n// editor-store.ts — removeRoofZone\nremoveRoofZone: (id) =>\n      commit((l) => {\n        if (!l.roofZones) return;\n        l.roofZones = l.roofZones.filter((z) => z.id !== id);\n        if (l.roofZones.length === 0) delete l.roofZones;\n      }),\n\n    splitRoofZone: (id, axis) => {\n      const { layout } = get();\n      const zone = layout?.roofZones?.find((z) => z.id === id);\n      if (!zone) return;\n      if (axis === \"x\" && zone.widthM < MIN_ROOF_ZONE * 2) return;\n      if (axis === \"y\" && zone.depthM < MIN_ROOF_ZONE * 2) return;\n\n      const firstId = `roofz-${nanoid(8)}`;\n      const secondId = `roofz-${nanoid(8)}`;\n      commit((l) => {\n        const index = l.roofZones?.findIndex((z) => z.id === id) ?? -1;\n        if (!l.roofZones || index < 0) return;\n        const base = l.roofZones[index];\n        const halfW = round2(base.widthM / 2);\n        const halfD = round2(base.depthM / 2);\n        const first: RoofZone"
 },
 {
  "id": "action:removeRoofZone",
  "kind": "action",
  "name": "removeRoofZone",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"removeRoofZone\"), id: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"removeRoofZone\",\"id\":\"<id zona>\"} — hapus zona atap eksplisit.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"removeRoofZone\") {\r\n        if (!store.layout?.roofZones?.some((z) => z.id === a.id)) continue\r\n        store.removeRoofZone(a.id)\r\n      } else if (a.type === \"setRooftop\") {\r\n        if (!store.layout) continue\r\n        store.setRooftop(a.enabled)\r\n      } else if (a.type === \"setWallCladding\") {\r\n        const parsed = parseOpeningWall(a.wallId)\r\n        if (!parsed || !store.layout?.rooms.some((r) => r.id === parsed.roomId)) continue\r\n        store.setWallCladding(a.wallId, a.cla\n\n// editor-store.ts — removeRoofZone\nremoveRoofZone: (id) =>\n      commit((l) => {\n        if (!l.roofZones) return;\n        l.roofZones = l.roofZones.filter((z) => z.id !== id);\n        if (l.roofZones.length === 0) delete l.roofZones;\n      }),\n\n    splitRoofZone: (id, axis) => {\n      const { layout } = get();\n      const zone = layout?.roofZones?.find((z) => z.id === id);\n      if (!zone) return;\n      if (axis === \"x\" && zone.widthM < MIN_ROOF_ZONE * 2) return;\n      if (axis === \"y\" && zone.depthM < MIN_ROOF_ZONE * 2) return;\n\n      const firstId = `roofz-${nanoid(8)}`;\n      const secondId = `roofz-${nanoid(8)}`;\n      commit((l) => {\n        const index = l.roofZones?.findIndex((z) => z.id === id) ?? -1;\n        if (!l.roofZones || index < 0) return;\n        const base = l.roofZones[index];\n        const halfW = round2(base.widthM / 2);\n        const halfD = round2(base.depthM / 2);\n        const first: RoofZone\n\n// editor-store.ts — setRooftop\nsetRooftop: (enabled) => {\n      const { layout } = get();\n      if (!layout) return;\n      const has = layout.floors.some((f) => f.id === \"floor-rooftop\");\n      if (enabled === has) return;\n      commit((l) => {\n        if (enabled) {\n          l.floors.push({\n            id: \"floor-rooftop\",\n            level: l.floors.length + 1,\n            name: \"Rooftop\",\n            heightM: 0.3, // slab tipis — rooftop = dak terbuka (paritas generator)\n          });\n        } else {\n          l.floors = l.floors.filter((f) => f.id !== \"floor-rooftop\");\n          l.rooms = l.rooms.filter((r) => r.floorId !== \"floor-rooftop\");\n          l.openings = l.openings.filter((o) => o.floorId !== \"floor-rooftop\");\n          if (l.roofZones) {\n            l.roofZones = l.roofZones.filter((z) => z.floorId !== \"floor-rooftop\");\n            if (l.roofZones.length === 0) delete l.roofZones;\n          }"
 },
 {
  "id": "action:setRooftop",
  "kind": "action",
  "name": "setRooftop",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"setRooftop\"), enabled: z.boolean() }),\n\n// dokumentasi prompt\n'- {\"type\":\"setRooftop\",\"enabled\":true|false} — aktif/nonaktifkan lantai dak rooftop (\"floor-rooftop\"). true = tambah lantai dak yang bisa diakses; false = hapus lantai rooftop beserta isinya. WAJIB aktif dulu sebelum setRooftopArea.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"setRooftop\") {\r\n        if (!store.layout) continue\r\n        store.setRooftop(a.enabled)\r\n      } else if (a.type === \"setWallCladding\") {\r\n        const parsed = parseOpeningWall(a.wallId)\r\n        if (!parsed || !store.layout?.rooms.some((r) => r.id === parsed.roomId)) continue\r\n        store.setWallCladding(a.wallId, a.claddingId, a.face ?? \"outer\")\r\n      } else if (a.type === \"addFacadeElement\") {\r\n        const parsed = parseOpeningWall(a.wallId)\r\n        if (!parsed || !store.\n\n// editor-store.ts — setRooftop\nsetRooftop: (enabled) => {\n      const { layout } = get();\n      if (!layout) return;\n      const has = layout.floors.some((f) => f.id === \"floor-rooftop\");\n      if (enabled === has) return;\n      commit((l) => {\n        if (enabled) {\n          l.floors.push({\n            id: \"floor-rooftop\",\n            level: l.floors.length + 1,\n            name: \"Rooftop\",\n            heightM: 0.3, // slab tipis — rooftop = dak terbuka (paritas generator)\n          });\n        } else {\n          l.floors = l.floors.filter((f) => f.id !== \"floor-rooftop\");\n          l.rooms = l.rooms.filter((r) => r.floorId !== \"floor-rooftop\");\n          l.openings = l.openings.filter((o) => o.floorId !== \"floor-rooftop\");\n          if (l.roofZones) {\n            l.roofZones = l.roofZones.filter((z) => z.floorId !== \"floor-rooftop\");\n            if (l.roofZones.length === 0) delete l.roofZones;\n          }\n\n// editor-store.ts — setWallCladding\nsetWallCladding: (wallId, claddingId, face = \"outer\") =>\n      commit((l) => {\n        const key = face === \"inner\" ? \"facadeInner\" : \"facade\";\n        const map = { ...(l[key] ?? {}) };\n        if (claddingId) map[wallId] = claddingId;\n        else delete map[wallId];\n        if (Object.keys(map).length === 0) delete l[key];\n        else l[key] = map;\n      }),\n\n    applyFacadePreset: (presetId) =>\n      commit((l) => {\n        // Gaya Fasad 1-klik: GANTI komposisi muka luar + kisi aksen (bukan\n        // menambah) — fasad advance seketika, lalu user tinggal tweak per dinding.\n        // `site` lokal store hanya lebar/dalam; runtime menerima project.site\n        // penuh (punya frontOrientation) — baca via cast, default \"s\".\n        const front = orientationToFrontSide(\n          (get().site as { frontOrientation?: string } | null)\n            ?.frontOrientation,\n        );"
 },
 {
  "id": "action:setRooftopArea",
  "kind": "action",
  "name": "setRooftopArea",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"setRooftopArea\"), area: rooftopAreaSchema }),\n\n// dokumentasi prompt\n'- {\"type\":\"setRooftopArea\",\"area\":{\"x\":<m>,\"y\":<m>,\"width\":<m>,\"depth\":<m>}} — atur area DECK rooftop (koordinat meter ABSOLUT di lahan, seperti ruang); sisa footprint otomatis jadi atap. Hanya untuk rumah dengan rooftop aktif (lihat DENAH.rooftopEnabled; kalau false, kirim setRooftop dulu); otomatis dijepit ke dalam footprint.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"setRooftopArea\") {\r\n        // Store clamps the rect to the building footprint — do NOT clamp here.\r\n        store.setRooftopArea(a.area)\r\n      } else if (a.type === \"clearRooftopArea\") {\r\n        store.setRooftopArea(undefined)\r\n      } else if (a.type === \"setSoilBearing\") {\r\n        store.setSoilBearing(a.soilBearingKPa)\r\n      } else if (a.type === \"addElectricalPoint\") {\r\n        if (!store.layout?.rooms.some((r) => r.id === a.roomId)) continue\r\n        store.addElectricalPoint\n\n// editor-store.ts — setRooftopArea\nsetRooftopArea: (area) =>\n      commit((l) => {\n        if (!area) {\n          delete l.rooftopArea;\n          return;\n        }\n        l.rooftopArea = clampRooftopArea(area, buildingFootprint(l));\n      }),\n\n    addRooftopTerrace: () =>\n      commit((l) => {\n        if (!l.floors.some((f) => f.id === \"floor-rooftop\")) return;\n        if (\n          l.rooms.some(\n            (r) => r.floorId === \"floor-rooftop\" && r.type === \"rooftop_lounge\",\n          )\n        )\n          return;\n        const fp = buildingFootprint(l);\n        if (fp.widthM <= 0 || fp.depthM <= 0) return;\n        // Teras mengikuti dak: rect deck parsial bila ada, selain itu footprint penuh.\n        const rect = l.rooftopArea\n          ? clampRooftopArea(l.rooftopArea, fp)\n          : { x: fp.x0, y: fp.y0, width: fp.widthM, depth: fp.depthM };\n        l.rooms.push({\n          id: `room-${nanoid(8)}`,\n          fl\n\n// editor-store.ts — setSoilBearing\nsetSoilBearing: (kPa) =>\n      commit((l) => {\n        l.structural = { soilBearingKPa: kPa };\n      }),\n\n    addElectricalPoint: (roomId, type, x, y) =>\n      commit((l) => {\n        const point: ElectricalPoint = {\n          id: `elec-${nanoid(8)}`,\n          roomId,\n          type,\n          x,\n          y,\n        };\n        l.electrical = [...(l.electrical ?? []), point];\n      }),\n\n    moveElectricalPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateElectricalPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeElectricalPoint: (id) =>\n      commit((l) => {\n        l.electrical = (l.electrical ?? []).filter(("
 },
 {
  "id": "action:clearRooftopArea",
  "kind": "action",
  "name": "clearRooftopArea",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"clearRooftopArea\") }),\n\n// dokumentasi prompt\n'- {\"type\":\"clearRooftopArea\"} — kembalikan rooftop ke deck penuh (hapus area parsial).\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"clearRooftopArea\") {\r\n        store.setRooftopArea(undefined)\r\n      } else if (a.type === \"setSoilBearing\") {\r\n        store.setSoilBearing(a.soilBearingKPa)\r\n      } else if (a.type === \"addElectricalPoint\") {\r\n        if (!store.layout?.rooms.some((r) => r.id === a.roomId)) continue\r\n        store.addElectricalPoint(a.roomId, a.pointType, a.x, a.y)\r\n      } else if (a.type === \"moveElectricalPoint\") {\r\n        if (!store.layout?.electrical?.some((p) => p.id === a.id)) continue\n\n// editor-store.ts — setRooftopArea\nsetRooftopArea: (area) =>\n      commit((l) => {\n        if (!area) {\n          delete l.rooftopArea;\n          return;\n        }\n        l.rooftopArea = clampRooftopArea(area, buildingFootprint(l));\n      }),\n\n    addRooftopTerrace: () =>\n      commit((l) => {\n        if (!l.floors.some((f) => f.id === \"floor-rooftop\")) return;\n        if (\n          l.rooms.some(\n            (r) => r.floorId === \"floor-rooftop\" && r.type === \"rooftop_lounge\",\n          )\n        )\n          return;\n        const fp = buildingFootprint(l);\n        if (fp.widthM <= 0 || fp.depthM <= 0) return;\n        // Teras mengikuti dak: rect deck parsial bila ada, selain itu footprint penuh.\n        const rect = l.rooftopArea\n          ? clampRooftopArea(l.rooftopArea, fp)\n          : { x: fp.x0, y: fp.y0, width: fp.widthM, depth: fp.depthM };\n        l.rooms.push({\n          id: `room-${nanoid(8)}`,\n          fl\n\n// editor-store.ts — setSoilBearing\nsetSoilBearing: (kPa) =>\n      commit((l) => {\n        l.structural = { soilBearingKPa: kPa };\n      }),\n\n    addElectricalPoint: (roomId, type, x, y) =>\n      commit((l) => {\n        const point: ElectricalPoint = {\n          id: `elec-${nanoid(8)}`,\n          roomId,\n          type,\n          x,\n          y,\n        };\n        l.electrical = [...(l.electrical ?? []), point];\n      }),\n\n    moveElectricalPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateElectricalPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeElectricalPoint: (id) =>\n      commit((l) => {\n        l.electrical = (l.electrical ?? []).filter(("
 },
 {
  "id": "action:setWallCladding",
  "kind": "action",
  "name": "setWallCladding",
  "evidence": "// skema (actions.ts)\nz.object({\r\n    type: z.literal(\"setWallCladding\"),\r\n    wallId: z.string().min(1),\r\n    claddingId: facadeCladdingIdSchema.nullable(),\r\n    face: facadeFaceSchema.optional(),\r\n  }),\n\n// dokumentasi prompt\n'- {\"type\":\"setWallCladding\",\"wallId\":\"<roomId>:<n|e|s|w>\",\"claddingId\":\"<id cladding>|null\",\"face?\":\"outer|inner\"} — material pelapis MUKA dinding (fasad). face default \"outer\" (muka luar); \"inner\" = aksen muka dalam dinding itu saja. claddingId null = kembali polos/cat. Cladding aktif saat ini ada di DENAH.facade / DENAH.facadeInner.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"setWallCladding\") {\r\n        const parsed = parseOpeningWall(a.wallId)\r\n        if (!parsed || !store.layout?.rooms.some((r) => r.id === parsed.roomId)) continue\r\n        store.setWallCladding(a.wallId, a.claddingId, a.face ?? \"outer\")\r\n      } else if (a.type === \"addFacadeElement\") {\r\n        const parsed = parseOpeningWall(a.wallId)\r\n        if (!parsed || !store.layout?.rooms.some((r) => r.id === parsed.roomId)) continue\r\n        store.addFacadeElement(a.wallId)\r\n      } else if\n\n// editor-store.ts — setWallCladding\nsetWallCladding: (wallId, claddingId, face = \"outer\") =>\n      commit((l) => {\n        const key = face === \"inner\" ? \"facadeInner\" : \"facade\";\n        const map = { ...(l[key] ?? {}) };\n        if (claddingId) map[wallId] = claddingId;\n        else delete map[wallId];\n        if (Object.keys(map).length === 0) delete l[key];\n        else l[key] = map;\n      }),\n\n    applyFacadePreset: (presetId) =>\n      commit((l) => {\n        // Gaya Fasad 1-klik: GANTI komposisi muka luar + kisi aksen (bukan\n        // menambah) — fasad advance seketika, lalu user tinggal tweak per dinding.\n        // `site` lokal store hanya lebar/dalam; runtime menerima project.site\n        // penuh (punya frontOrientation) — baca via cast, default \"s\".\n        const front = orientationToFrontSide(\n          (get().site as { frontOrientation?: string } | null)\n            ?.frontOrientation,\n        );\n\n// editor-store.ts — addFacadeElement\naddFacadeElement: (wallId, kind = \"louver_band\") =>\n      commit((l) => {\n        const parsed = parseOpeningWall(wallId);\n        const host = parsed\n          ? l.rooms.find((r) => r.id === parsed.roomId)\n          : null;\n        if (!parsed || !host) return;\n        const len =\n          parsed.side === \"n\" || parsed.side === \"s\" ? host.width : host.depth;\n        const widthM = Math.max(0.6, Math.round((len - 0.2) * 100) / 100);\n        l.facadeElements = [\n          ...(l.facadeElements ?? []),\n          {\n            id: `fe-${Math.abs(hashStr(wallId + kind + (l.facadeElements?.length ?? 0)))}`,\n            wallId,\n            floorId: host.floorId,\n            kind,\n            positionM: Math.round((len / 2) * 100) / 100,\n            widthM,\n            sillHeightM: 0.3,\n            heightM: 2.2,\n            finish: \"kayu\",\n          },\n        ];\n      }),\n\n    updateFacade"
 },
 {
  "id": "action:addFacadeElement",
  "kind": "action",
  "name": "addFacadeElement",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"addFacadeElement\"), wallId: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"addFacadeElement\",\"wallId\":\"<roomId>:<n|e|s|w>\"} — tambah LOUVER BAND (kisi-kisi vertikal lebar, khas fasad modern) di muka luar dinding itu; default selebar dinding, sill 0.3 m, tinggi 2.2 m.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"addFacadeElement\") {\r\n        const parsed = parseOpeningWall(a.wallId)\r\n        if (!parsed || !store.layout?.rooms.some((r) => r.id === parsed.roomId)) continue\r\n        store.addFacadeElement(a.wallId)\r\n      } else if (a.type === \"updateFacadeElement\") {\r\n        if (!store.layout?.facadeElements?.some((fe) => fe.id === a.id)) continue\r\n        store.updateFacadeElement(a.id, a.patch)\r\n      } else if (a.type === \"removeFacadeElement\") {\r\n        if (!store.layout?.facadeElements\n\n// editor-store.ts — addFacadeElement\naddFacadeElement: (wallId, kind = \"louver_band\") =>\n      commit((l) => {\n        const parsed = parseOpeningWall(wallId);\n        const host = parsed\n          ? l.rooms.find((r) => r.id === parsed.roomId)\n          : null;\n        if (!parsed || !host) return;\n        const len =\n          parsed.side === \"n\" || parsed.side === \"s\" ? host.width : host.depth;\n        const widthM = Math.max(0.6, Math.round((len - 0.2) * 100) / 100);\n        l.facadeElements = [\n          ...(l.facadeElements ?? []),\n          {\n            id: `fe-${Math.abs(hashStr(wallId + kind + (l.facadeElements?.length ?? 0)))}`,\n            wallId,\n            floorId: host.floorId,\n            kind,\n            positionM: Math.round((len / 2) * 100) / 100,\n            widthM,\n            sillHeightM: 0.3,\n            heightM: 2.2,\n            finish: \"kayu\",\n          },\n        ];\n      }),\n\n    updateFacade\n\n// editor-store.ts — updateFacadeElement\nupdateFacadeElement: (id, patch) =>\n      commit((l) => {\n        l.facadeElements = (l.facadeElements ?? []).map((fe) =>\n          fe.id === id ? { ...fe, ...patch, id: fe.id } : fe,\n        );\n      }),\n\n    removeFacadeElement: (id) =>\n      commit((l) => {\n        const next = (l.facadeElements ?? []).filter((fe) => fe.id !== id);\n        if (next.length === 0) delete l.facadeElements;\n        else l.facadeElements = next;\n      }),\n\n    addExteriorElement: (element) =>\n      commit((l) => {\n        l.exteriorElements = [\n          ...(l.exteriorElements ?? []),\n          element,\n        ] as ExteriorElement[];\n      }),\n\n    updateExteriorElement: (id, patch) =>\n      commit((l) => {\n        l.exteriorElements = (l.exteriorElements ?? []).map((el) =>\n          el.id === id\n            ? ({ ...el, ...patch, id: el.id } as ExteriorElement)\n            : el,\n        );\n      }),"
 },
 {
  "id": "action:updateFacadeElement",
  "kind": "action",
  "name": "updateFacadeElement",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"updateFacadeElement\"), id: z.string().min(1), patch: facadeElementPatchSchema }),\n\n// dokumentasi prompt\n'- {\"type\":\"updateFacadeElement\",\"id\":\"<id elemen>\",\"patch\":{positionM?,widthM?,sillHeightM?,heightM?,kind?:\"louver_band|slat_horizontal|roster_screen\",finish?:\"kayu|aluminium_gelap|putih\",modelUrl?,modelAssetId?}} — ubah elemen fasad; modelUrl/modelAssetId = GLB kustom dari library (null = lepas, kembali procedural); id dari DENAH.facadeElements.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"updateFacadeElement\") {\r\n        if (!store.layout?.facadeElements?.some((fe) => fe.id === a.id)) continue\r\n        store.updateFacadeElement(a.id, a.patch)\r\n      } else if (a.type === \"removeFacadeElement\") {\r\n        if (!store.layout?.facadeElements?.some((fe) => fe.id === a.id)) continue\r\n        store.removeFacadeElement(a.id)\r\n      } else if (a.type === \"addExteriorElement\") {\r\n        const draft = a.element\r\n        let element: ExteriorElement\r\n        if (\"start\" in draft\n\n// editor-store.ts — updateFacadeElement\nupdateFacadeElement: (id, patch) =>\n      commit((l) => {\n        l.facadeElements = (l.facadeElements ?? []).map((fe) =>\n          fe.id === id ? { ...fe, ...patch, id: fe.id } : fe,\n        );\n      }),\n\n    removeFacadeElement: (id) =>\n      commit((l) => {\n        const next = (l.facadeElements ?? []).filter((fe) => fe.id !== id);\n        if (next.length === 0) delete l.facadeElements;\n        else l.facadeElements = next;\n      }),\n\n    addExteriorElement: (element) =>\n      commit((l) => {\n        l.exteriorElements = [\n          ...(l.exteriorElements ?? []),\n          element,\n        ] as ExteriorElement[];\n      }),\n\n    updateExteriorElement: (id, patch) =>\n      commit((l) => {\n        l.exteriorElements = (l.exteriorElements ?? []).map((el) =>\n          el.id === id\n            ? ({ ...el, ...patch, id: el.id } as ExteriorElement)\n            : el,\n        );\n      }),\n\n// editor-store.ts — removeFacadeElement\nremoveFacadeElement: (id) =>\n      commit((l) => {\n        const next = (l.facadeElements ?? []).filter((fe) => fe.id !== id);\n        if (next.length === 0) delete l.facadeElements;\n        else l.facadeElements = next;\n      }),\n\n    addExteriorElement: (element) =>\n      commit((l) => {\n        l.exteriorElements = [\n          ...(l.exteriorElements ?? []),\n          element,\n        ] as ExteriorElement[];\n      }),\n\n    updateExteriorElement: (id, patch) =>\n      commit((l) => {\n        l.exteriorElements = (l.exteriorElements ?? []).map((el) =>\n          el.id === id\n            ? ({ ...el, ...patch, id: el.id } as ExteriorElement)\n            : el,\n        );\n      }),\n\n    removeExteriorElement: (id) =>\n      commit((l) => {\n        const next = (l.exteriorElements ?? []).filter((el) => el.id !== id);\n        if (next.length === 0) delete l.exteriorElements;\n        else l.ex"
 },
 {
  "id": "action:removeFacadeElement",
  "kind": "action",
  "name": "removeFacadeElement",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"removeFacadeElement\"), id: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"removeFacadeElement\",\"id\":\"<id elemen>\"} — hapus louver band.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"removeFacadeElement\") {\r\n        if (!store.layout?.facadeElements?.some((fe) => fe.id === a.id)) continue\r\n        store.removeFacadeElement(a.id)\r\n      } else if (a.type === \"addExteriorElement\") {\r\n        const draft = a.element\r\n        let element: ExteriorElement\r\n        if (\"start\" in draft) {\r\n          element = makeSegmentElement(draft.kind, draft.start, draft.end, {\r\n            floorId: draft.floorId,\r\n            label: draft.label,\r\n            heightM: draft.heightM\n\n// editor-store.ts — removeFacadeElement\nremoveFacadeElement: (id) =>\n      commit((l) => {\n        const next = (l.facadeElements ?? []).filter((fe) => fe.id !== id);\n        if (next.length === 0) delete l.facadeElements;\n        else l.facadeElements = next;\n      }),\n\n    addExteriorElement: (element) =>\n      commit((l) => {\n        l.exteriorElements = [\n          ...(l.exteriorElements ?? []),\n          element,\n        ] as ExteriorElement[];\n      }),\n\n    updateExteriorElement: (id, patch) =>\n      commit((l) => {\n        l.exteriorElements = (l.exteriorElements ?? []).map((el) =>\n          el.id === id\n            ? ({ ...el, ...patch, id: el.id } as ExteriorElement)\n            : el,\n        );\n      }),\n\n    removeExteriorElement: (id) =>\n      commit((l) => {\n        const next = (l.exteriorElements ?? []).filter((el) => el.id !== id);\n        if (next.length === 0) delete l.exteriorElements;\n        else l.ex"
 },
 {
  "id": "action:addExteriorElement",
  "kind": "action",
  "name": "addExteriorElement",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"addExteriorElement\"), element: exteriorDraftSchema }),\n\n// dokumentasi prompt\n'- {\"type\":\"addExteriorElement\",\"element\":{...}} — tambah elemen eksterior native. kind legal: boundary_wall/fence/sliding_gate/swing_gate/pedestrian_gate; solid_wall/facade_panel/column/beam/slab/canopy/planter; portal_frame; exterior_stair; driveway/walkway/terrace_surface/garden_bed; asset/plant/tree/exterior_decor/vehicle. Segment memakai start/end/heightM, box memakai x/y/widthM/depthM/heightM, surface memakai points (maks 16), asset/plant/tree/decor/vehicle memakai envelope x/y/widthM/depthM/heightM dan optional modelUrl.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"addExteriorElement\") {\r\n        const draft = a.element\r\n        let element: ExteriorElement\r\n        if (\"start\" in draft) {\r\n          element = makeSegmentElement(draft.kind, draft.start, draft.end, {\r\n            floorId: draft.floorId,\r\n            label: draft.label,\r\n            heightM: draft.heightM,\r\n            thicknessM: draft.thicknessM,\r\n            material: draft.material,\r\n          })\r\n        } else if (draft.kind === \"portal_frame\") {\r\n          element = makeFr"
 },
 {
  "id": "action:updateExteriorElement",
  "kind": "action",
  "name": "updateExteriorElement",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"updateExteriorElement\"), id: z.string().min(1), patch: exteriorPatchSchema }),\n\n// dokumentasi prompt\n'- {\"type\":\"updateExteriorElement\",\"id\":\"<id>\",\"patch\":{...}} — ubah geometry/material elemen eksterior yang ada; id/kind tidak dapat diubah.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"updateExteriorElement\") {\r\n        if (!store.layout?.exteriorElements?.some((element) => element.id === a.id)) continue\r\n        store.updateExteriorElement(a.id, a.patch as Partial<ExteriorElement>)\r\n      } else if (a.type === \"removeExteriorElement\") {\r\n        if (!store.layout?.exteriorElements?.some((element) => element.id === a.id)) continue\r\n        store.removeExteriorElement(a.id)\r\n      } else if (a.type === \"applyFacadeTemplate\") {\r\n        store.applyExteriorTemplate(a.\n\n// editor-store.ts — updateExteriorElement\nupdateExteriorElement: (id, patch) =>\n      commit((l) => {\n        l.exteriorElements = (l.exteriorElements ?? []).map((el) =>\n          el.id === id\n            ? ({ ...el, ...patch, id: el.id } as ExteriorElement)\n            : el,\n        );\n      }),\n\n    removeExteriorElement: (id) =>\n      commit((l) => {\n        const next = (l.exteriorElements ?? []).filter((el) => el.id !== id);\n        if (next.length === 0) delete l.exteriorElements;\n        else l.exteriorElements = next as ExteriorElement[];\n      }),\n\n    duplicateExteriorElement: (id) =>\n      commit((l) => {\n        const el = l.exteriorElements?.find((e) => e.id === id);\n        if (!el) return;\n        const copy = clone(el);\n        copy.id = `ext-${nanoid(8)}`;\n        copy.label = el.label ? `${el.label} (copy)` : `${copy.kind} (copy)`;\n        if (\"x\" in copy && typeof copy.x === \"number\") copy.x += 0.5;\n\n// editor-store.ts — removeExteriorElement\nremoveExteriorElement: (id) =>\n      commit((l) => {\n        const next = (l.exteriorElements ?? []).filter((el) => el.id !== id);\n        if (next.length === 0) delete l.exteriorElements;\n        else l.exteriorElements = next as ExteriorElement[];\n      }),\n\n    duplicateExteriorElement: (id) =>\n      commit((l) => {\n        const el = l.exteriorElements?.find((e) => e.id === id);\n        if (!el) return;\n        const copy = clone(el);\n        copy.id = `ext-${nanoid(8)}`;\n        copy.label = el.label ? `${el.label} (copy)` : `${copy.kind} (copy)`;\n        if (\"x\" in copy && typeof copy.x === \"number\") copy.x += 0.5;\n        if (\"y\" in copy && typeof copy.y === \"number\") copy.y += 0.5;\n        if (\"start\" in copy && copy.start) {\n          copy.start.x += 0.5;\n          copy.start.y += 0.5;\n        }\n        if (\"end\" in copy && copy.end) {\n          copy.end.x += 0.5;"
 },
 {
  "id": "action:removeExteriorElement",
  "kind": "action",
  "name": "removeExteriorElement",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"removeExteriorElement\"), id: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"removeExteriorElement\",\"id\":\"<id>\"} — hapus elemen eksterior native.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"removeExteriorElement\") {\r\n        if (!store.layout?.exteriorElements?.some((element) => element.id === a.id)) continue\r\n        store.removeExteriorElement(a.id)\r\n      } else if (a.type === \"applyFacadeTemplate\") {\r\n        store.applyExteriorTemplate(a.templateId)\r\n      } else if (a.type === \"addWallLamp\") {\r\n        const parsed = parseOpeningWall(a.wallId)\r\n        if (!parsed || !store.layout?.rooms.some((r) => r.id === parsed.roomId)) continue\r\n        store.addWallLamp(a.wa\n\n// editor-store.ts — removeExteriorElement\nremoveExteriorElement: (id) =>\n      commit((l) => {\n        const next = (l.exteriorElements ?? []).filter((el) => el.id !== id);\n        if (next.length === 0) delete l.exteriorElements;\n        else l.exteriorElements = next as ExteriorElement[];\n      }),\n\n    duplicateExteriorElement: (id) =>\n      commit((l) => {\n        const el = l.exteriorElements?.find((e) => e.id === id);\n        if (!el) return;\n        const copy = clone(el);\n        copy.id = `ext-${nanoid(8)}`;\n        copy.label = el.label ? `${el.label} (copy)` : `${copy.kind} (copy)`;\n        if (\"x\" in copy && typeof copy.x === \"number\") copy.x += 0.5;\n        if (\"y\" in copy && typeof copy.y === \"number\") copy.y += 0.5;\n        if (\"start\" in copy && copy.start) {\n          copy.start.x += 0.5;\n          copy.start.y += 0.5;\n        }\n        if (\"end\" in copy && copy.end) {\n          copy.end.x += 0.5;\n\n// editor-store.ts — applyExteriorTemplate\napplyExteriorTemplate: (templateId) =>\n      commit((l) => {\n        const site = get().site;\n        if (!site) return;\n        const result = buildFacadeComposerTemplate(l, site, templateId);\n        const manualElements = (l.exteriorElements ?? []).filter(\n          (element) => !element.label?.startsWith(\"Template:\"),\n        );\n        l.exteriorElements = [...manualElements, ...result.exteriorElements];\n        if (Object.keys(result.facade).length > 0) l.facade = result.facade;\n        else delete l.facade;\n        if (result.facadeElements.length > 0)\n          l.facadeElements = result.facadeElements;\n        else delete l.facadeElements;\n      }),\n\n    addFacadeElement: (wallId, kind = \"louver_band\") =>\n      commit((l) => {\n        const parsed = parseOpeningWall(wallId);\n        const host = parsed\n          ? l.rooms.find((r) => r.id === parsed.roomId)\n          : null;"
 },
 {
  "id": "action:applyFacadeTemplate",
  "kind": "action",
  "name": "applyFacadeTemplate",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"applyFacadeTemplate\"), templateId: z.enum(FACADE_COMPOSER_TEMPLATE_IDS) }),\n\n// dokumentasi prompt\n'- {\"type\":\"applyFacadeTemplate\",\"templateId\":\"modern_concrete_vertical|brick_gable_roster|minimalist_portal_carport\"} — terapkan komposisi tampak depan native sebagai satu perubahan undoable; gunakan saat user meminta gaya siap pakai.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"applyFacadeTemplate\") {\r\n        store.applyExteriorTemplate(a.templateId)\r\n      } else if (a.type === \"addWallLamp\") {\r\n        const parsed = parseOpeningWall(a.wallId)\r\n        if (!parsed || !store.layout?.rooms.some((r) => r.id === parsed.roomId)) continue\r\n        store.addWallLamp(a.wallId)\r\n      } else if (a.type === \"updateLamp\") {\r\n        // Validate against the EFFECTIVE list — the store copy-on-writes the\r\n        // auto placement, so auto ids are legal targets too.\n\n// editor-store.ts — applyExteriorTemplate\napplyExteriorTemplate: (templateId) =>\n      commit((l) => {\n        const site = get().site;\n        if (!site) return;\n        const result = buildFacadeComposerTemplate(l, site, templateId);\n        const manualElements = (l.exteriorElements ?? []).filter(\n          (element) => !element.label?.startsWith(\"Template:\"),\n        );\n        l.exteriorElements = [...manualElements, ...result.exteriorElements];\n        if (Object.keys(result.facade).length > 0) l.facade = result.facade;\n        else delete l.facade;\n        if (result.facadeElements.length > 0)\n          l.facadeElements = result.facadeElements;\n        else delete l.facadeElements;\n      }),\n\n    addFacadeElement: (wallId, kind = \"louver_band\") =>\n      commit((l) => {\n        const parsed = parseOpeningWall(wallId);\n        const host = parsed\n          ? l.rooms.find((r) => r.id === parsed.roomId)\n          : null;\n\n// editor-store.ts — addWallLamp\naddWallLamp: (wallId) =>\n      commit((l) => {\n        const parsed = parseOpeningWall(wallId);\n        const host = parsed\n          ? l.rooms.find((r) => r.id === parsed.roomId)\n          : null;\n        if (!parsed || !host) return;\n        const horizontal = parsed.side === \"n\" || parsed.side === \"s\";\n        const line =\n          parsed.side === \"n\"\n            ? host.y\n            : parsed.side === \"s\"\n              ? host.y + host.depth\n              : parsed.side === \"w\"\n                ? host.x\n                : host.x + host.width;\n        const mid = horizontal\n          ? host.x + host.width / 2\n          : host.y + host.depth / 2;\n        const lamps = l.exteriorLamps ?? lampPlacements(l);\n        l.exteriorLamps = [\n          ...lamps,\n          {\n            id: `lamp-w-${Math.abs(hashStr(wallId + lamps.length))}`,\n            kind: \"wall\",\n            x: horizontal ?"
 },
 {
  "id": "action:addWallLamp",
  "kind": "action",
  "name": "addWallLamp",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"addWallLamp\"), wallId: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"addWallLamp\",\"wallId\":\"<roomId>:<n|e|s|w>\"} — tambah lampu dinding eksterior di tengah dinding itu.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"addWallLamp\") {\r\n        const parsed = parseOpeningWall(a.wallId)\r\n        if (!parsed || !store.layout?.rooms.some((r) => r.id === parsed.roomId)) continue\r\n        store.addWallLamp(a.wallId)\r\n      } else if (a.type === \"updateLamp\") {\r\n        // Validate against the EFFECTIVE list — the store copy-on-writes the\r\n        // auto placement, so auto ids are legal targets too.\r\n        if (!store.layout || !effectiveLamps(store.layout).some((l) => l.id === a.id)) continue\n\n// editor-store.ts — addWallLamp\naddWallLamp: (wallId) =>\n      commit((l) => {\n        const parsed = parseOpeningWall(wallId);\n        const host = parsed\n          ? l.rooms.find((r) => r.id === parsed.roomId)\n          : null;\n        if (!parsed || !host) return;\n        const horizontal = parsed.side === \"n\" || parsed.side === \"s\";\n        const line =\n          parsed.side === \"n\"\n            ? host.y\n            : parsed.side === \"s\"\n              ? host.y + host.depth\n              : parsed.side === \"w\"\n                ? host.x\n                : host.x + host.width;\n        const mid = horizontal\n          ? host.x + host.width / 2\n          : host.y + host.depth / 2;\n        const lamps = l.exteriorLamps ?? lampPlacements(l);\n        l.exteriorLamps = [\n          ...lamps,\n          {\n            id: `lamp-w-${Math.abs(hashStr(wallId + lamps.length))}`,\n            kind: \"wall\",\n            x: horizontal ?"
 },
 {
  "id": "action:updateLamp",
  "kind": "action",
  "name": "updateLamp",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"updateLamp\"), id: z.string().min(1), patch: lampPatchSchema }),\n\n// dokumentasi prompt\n'- {\"type\":\"updateLamp\",\"id\":\"<id lampu>\",\"patch\":{color?:\"#rrggbb\",intensity?:<0-2; 0=mati>,watt?:<1-100>,mountH?:<meter>,modelUrl?,modelAssetId?}} — ubah lampu eksterior; id dari DENAH.exteriorLamps (termasuk lampu penempatan otomatis — mengedit akan mematerialisasikannya). modelUrl/modelAssetId null = kembali ke fixture bawaan.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"updateLamp\") {\r\n        // Validate against the EFFECTIVE list — the store copy-on-writes the\r\n        // auto placement, so auto ids are legal targets too.\r\n        if (!store.layout || !effectiveLamps(store.layout).some((l) => l.id === a.id)) continue\r\n        store.updateLamp(a.id, a.patch)\r\n      } else if (a.type === \"removeLamp\") {\r\n        if (!store.layout || !effectiveLamps(store.layout).some((l) => l.id === a.id)) continue\r\n        store.removeLamp(a.id)\r\n      } else if (a\n\n// editor-store.ts — updateLamp\nupdateLamp: (id, patch) =>\n      commit((l) => {\n        const lamps = l.exteriorLamps ?? lampPlacements(l);\n        l.exteriorLamps = lamps.map((lamp) =>\n          lamp.id === id ? { ...lamp, ...patch, id: lamp.id } : lamp,\n        );\n      }),\n\n    removeLamp: (id) =>\n      commit((l) => {\n        const lamps = l.exteriorLamps ?? lampPlacements(l);\n        // Daftar dimaterialisasi meski hasilnya kosong — [] berarti \"user\n        // menghapus semuanya\", beda dari absent (= otomatis).\n        l.exteriorLamps = lamps.filter((lamp) => lamp.id !== id);\n      }),\n\n    setRooftopArea: (area) =>\n      commit((l) => {\n        if (!area) {\n          delete l.rooftopArea;\n          return;\n        }\n        l.rooftopArea = clampRooftopArea(area, buildingFootprint(l));\n      }),\n\n    addRooftopTerrace: () =>\n      commit((l) => {\n        if (!l.floors.some((f) => f.id === \"floor-rooftop\")) re\n\n// editor-store.ts — removeLamp\nremoveLamp: (id) =>\n      commit((l) => {\n        const lamps = l.exteriorLamps ?? lampPlacements(l);\n        // Daftar dimaterialisasi meski hasilnya kosong — [] berarti \"user\n        // menghapus semuanya\", beda dari absent (= otomatis).\n        l.exteriorLamps = lamps.filter((lamp) => lamp.id !== id);\n      }),\n\n    setRooftopArea: (area) =>\n      commit((l) => {\n        if (!area) {\n          delete l.rooftopArea;\n          return;\n        }\n        l.rooftopArea = clampRooftopArea(area, buildingFootprint(l));\n      }),\n\n    addRooftopTerrace: () =>\n      commit((l) => {\n        if (!l.floors.some((f) => f.id === \"floor-rooftop\")) return;\n        if (\n          l.rooms.some(\n            (r) => r.floorId === \"floor-rooftop\" && r.type === \"rooftop_lounge\",\n          )\n        )\n          return;\n        const fp = buildingFootprint(l);\n        if (fp.widthM <= 0 || fp.depthM <= 0)"
 },
 {
  "id": "action:removeLamp",
  "kind": "action",
  "name": "removeLamp",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"removeLamp\"), id: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"removeLamp\",\"id\":\"<id lampu>\"} — hapus lampu eksterior.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"removeLamp\") {\r\n        if (!store.layout || !effectiveLamps(store.layout).some((l) => l.id === a.id)) continue\r\n        store.removeLamp(a.id)\r\n      } else if (a.type === \"setRooftopArea\") {\r\n        // Store clamps the rect to the building footprint — do NOT clamp here.\r\n        store.setRooftopArea(a.area)\r\n      } else if (a.type === \"clearRooftopArea\") {\r\n        store.setRooftopArea(undefined)\r\n      } else if (a.type === \"setSoilBearing\") {\r\n        store.setSoilBearing(a.soi\n\n// editor-store.ts — removeLamp\nremoveLamp: (id) =>\n      commit((l) => {\n        const lamps = l.exteriorLamps ?? lampPlacements(l);\n        // Daftar dimaterialisasi meski hasilnya kosong — [] berarti \"user\n        // menghapus semuanya\", beda dari absent (= otomatis).\n        l.exteriorLamps = lamps.filter((lamp) => lamp.id !== id);\n      }),\n\n    setRooftopArea: (area) =>\n      commit((l) => {\n        if (!area) {\n          delete l.rooftopArea;\n          return;\n        }\n        l.rooftopArea = clampRooftopArea(area, buildingFootprint(l));\n      }),\n\n    addRooftopTerrace: () =>\n      commit((l) => {\n        if (!l.floors.some((f) => f.id === \"floor-rooftop\")) return;\n        if (\n          l.rooms.some(\n            (r) => r.floorId === \"floor-rooftop\" && r.type === \"rooftop_lounge\",\n          )\n        )\n          return;\n        const fp = buildingFootprint(l);\n        if (fp.widthM <= 0 || fp.depthM <= 0)\n\n// editor-store.ts — setRooftopArea\nsetRooftopArea: (area) =>\n      commit((l) => {\n        if (!area) {\n          delete l.rooftopArea;\n          return;\n        }\n        l.rooftopArea = clampRooftopArea(area, buildingFootprint(l));\n      }),\n\n    addRooftopTerrace: () =>\n      commit((l) => {\n        if (!l.floors.some((f) => f.id === \"floor-rooftop\")) return;\n        if (\n          l.rooms.some(\n            (r) => r.floorId === \"floor-rooftop\" && r.type === \"rooftop_lounge\",\n          )\n        )\n          return;\n        const fp = buildingFootprint(l);\n        if (fp.widthM <= 0 || fp.depthM <= 0) return;\n        // Teras mengikuti dak: rect deck parsial bila ada, selain itu footprint penuh.\n        const rect = l.rooftopArea\n          ? clampRooftopArea(l.rooftopArea, fp)\n          : { x: fp.x0, y: fp.y0, width: fp.widthM, depth: fp.depthM };\n        l.rooms.push({\n          id: `room-${nanoid(8)}`,\n          fl"
 },
 {
  "id": "action:setSoilBearing",
  "kind": "action",
  "name": "setSoilBearing",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"setSoilBearing\"), soilBearingKPa: z.number() }),\n\n// dokumentasi prompt\n'- {\"type\":\"setSoilBearing\",\"soilBearingKPa\":<50-400>} — set daya dukung tanah σ (kPa) untuk perhitungan struktur (pondasi/kolom).\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"setSoilBearing\") {\r\n        store.setSoilBearing(a.soilBearingKPa)\r\n      } else if (a.type === \"addElectricalPoint\") {\r\n        if (!store.layout?.rooms.some((r) => r.id === a.roomId)) continue\r\n        store.addElectricalPoint(a.roomId, a.pointType, a.x, a.y)\r\n      } else if (a.type === \"moveElectricalPoint\") {\r\n        if (!store.layout?.electrical?.some((p) => p.id === a.id)) continue\r\n        store.moveElectricalPoint(a.id, a.x, a.y)\r\n      } else if (a.type === \"updateElectric\n\n// editor-store.ts — setSoilBearing\nsetSoilBearing: (kPa) =>\n      commit((l) => {\n        l.structural = { soilBearingKPa: kPa };\n      }),\n\n    addElectricalPoint: (roomId, type, x, y) =>\n      commit((l) => {\n        const point: ElectricalPoint = {\n          id: `elec-${nanoid(8)}`,\n          roomId,\n          type,\n          x,\n          y,\n        };\n        l.electrical = [...(l.electrical ?? []), point];\n      }),\n\n    moveElectricalPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateElectricalPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeElectricalPoint: (id) =>\n      commit((l) => {\n        l.electrical = (l.electrical ?? []).filter((\n\n// editor-store.ts — addElectricalPoint\naddElectricalPoint: (roomId, type, x, y) =>\n      commit((l) => {\n        const point: ElectricalPoint = {\n          id: `elec-${nanoid(8)}`,\n          roomId,\n          type,\n          x,\n          y,\n        };\n        l.electrical = [...(l.electrical ?? []), point];\n      }),\n\n    moveElectricalPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateElectricalPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeElectricalPoint: (id) =>\n      commit((l) => {\n        l.electrical = (l.electrical ?? []).filter((p) => p.id !== id);\n      }),\n\n    setElectrical: (points) =>\n      commit((l) => {\n        l.electrical = poi"
 },
 {
  "id": "action:addElectricalPoint",
  "kind": "action",
  "name": "addElectricalPoint",
  "evidence": "// skema (actions.ts)\nz.object({\r\n    type: z.literal(\"addElectricalPoint\"),\r\n    roomId: z.string().min(1),\r\n    pointType: electricalPointTypeSchema,\r\n    x: z.number().finite(),\r\n    y: z.number().finite(),\r\n  }),\n\n// dokumentasi prompt\n'- {\"type\":\"addElectricalPoint\",\"roomId\":\"<id>\",\"pointType\":\"stopkontak|stopkontak_daya|saklar_tunggal|saklar_ganda|panel|data\",\"x\":<meter>,\"y\":<meter>} — tambah titik listrik (x/y = koordinat meter ABSOLUT di lahan).\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"addElectricalPoint\") {\r\n        if (!store.layout?.rooms.some((r) => r.id === a.roomId)) continue\r\n        store.addElectricalPoint(a.roomId, a.pointType, a.x, a.y)\r\n      } else if (a.type === \"moveElectricalPoint\") {\r\n        if (!store.layout?.electrical?.some((p) => p.id === a.id)) continue\r\n        store.moveElectricalPoint(a.id, a.x, a.y)\r\n      } else if (a.type === \"updateElectricalPoint\") {\r\n        if (!store.layout?.electrical?.some((p) => p.id === a.id)) continue\n\n// editor-store.ts — addElectricalPoint\naddElectricalPoint: (roomId, type, x, y) =>\n      commit((l) => {\n        const point: ElectricalPoint = {\n          id: `elec-${nanoid(8)}`,\n          roomId,\n          type,\n          x,\n          y,\n        };\n        l.electrical = [...(l.electrical ?? []), point];\n      }),\n\n    moveElectricalPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateElectricalPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeElectricalPoint: (id) =>\n      commit((l) => {\n        l.electrical = (l.electrical ?? []).filter((p) => p.id !== id);\n      }),\n\n    setElectrical: (points) =>\n      commit((l) => {\n        l.electrical = poi\n\n// editor-store.ts — moveElectricalPoint\nmoveElectricalPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateElectricalPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeElectricalPoint: (id) =>\n      commit((l) => {\n        l.electrical = (l.electrical ?? []).filter((p) => p.id !== id);\n      }),\n\n    setElectrical: (points) =>\n      commit((l) => {\n        l.electrical = points;\n      }),\n\n    addWaterPoint: (roomId, type, x, y) =>\n      commit((l) => {\n        const point: WaterPoint = {\n          id: `water-${nanoid(8)}`,\n          roomId,\n          type,\n          x,\n          y,\n        };\n        l.water = [...(l.water ?? []), point];\n      }),"
 },
 {
  "id": "action:moveElectricalPoint",
  "kind": "action",
  "name": "moveElectricalPoint",
  "evidence": "// skema (actions.ts)\nz.object({\r\n    type: z.literal(\"moveElectricalPoint\"),\r\n    id: z.string().min(1),\r\n    x: z.number().finite(),\r\n    y: z.number().finite(),\r\n  }),\n\n// dokumentasi prompt\n'- {\"type\":\"moveElectricalPoint\",\"id\":\"<id titik>\",\"x\":<meter>,\"y\":<meter>} — pindah titik listrik ke koordinat absolut.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"moveElectricalPoint\") {\r\n        if (!store.layout?.electrical?.some((p) => p.id === a.id)) continue\r\n        store.moveElectricalPoint(a.id, a.x, a.y)\r\n      } else if (a.type === \"updateElectricalPoint\") {\r\n        if (!store.layout?.electrical?.some((p) => p.id === a.id)) continue\r\n        store.updateElectricalPoint(a.id, a.patch)\r\n      } else if (a.type === \"removeElectricalPoint\") {\r\n        if (!store.layout?.electrical?.some((p) => p.id === a.id)) continue\r\n        store.rem\n\n// editor-store.ts — moveElectricalPoint\nmoveElectricalPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateElectricalPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeElectricalPoint: (id) =>\n      commit((l) => {\n        l.electrical = (l.electrical ?? []).filter((p) => p.id !== id);\n      }),\n\n    setElectrical: (points) =>\n      commit((l) => {\n        l.electrical = points;\n      }),\n\n    addWaterPoint: (roomId, type, x, y) =>\n      commit((l) => {\n        const point: WaterPoint = {\n          id: `water-${nanoid(8)}`,\n          roomId,\n          type,\n          x,\n          y,\n        };\n        l.water = [...(l.water ?? []), point];\n      }),\n\n// editor-store.ts — updateElectricalPoint\nupdateElectricalPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeElectricalPoint: (id) =>\n      commit((l) => {\n        l.electrical = (l.electrical ?? []).filter((p) => p.id !== id);\n      }),\n\n    setElectrical: (points) =>\n      commit((l) => {\n        l.electrical = points;\n      }),\n\n    addWaterPoint: (roomId, type, x, y) =>\n      commit((l) => {\n        const point: WaterPoint = {\n          id: `water-${nanoid(8)}`,\n          roomId,\n          type,\n          x,\n          y,\n        };\n        l.water = [...(l.water ?? []), point];\n      }),\n\n    moveWaterPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateWat"
 },
 {
  "id": "action:updateElectricalPoint",
  "kind": "action",
  "name": "updateElectricalPoint",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"updateElectricalPoint\"), id: z.string().min(1), patch: electricalPatchSchema }),\n\n// dokumentasi prompt\n'- {\"type\":\"updateElectricalPoint\",\"id\":\"<id titik>\",\"patch\":{type?:\"stopkontak|...\",\"roomId?\":\"<id>\",\"note?\":\"<teks>\"}} — ubah tipe/ruang/catatan titik listrik.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"updateElectricalPoint\") {\r\n        if (!store.layout?.electrical?.some((p) => p.id === a.id)) continue\r\n        store.updateElectricalPoint(a.id, a.patch)\r\n      } else if (a.type === \"removeElectricalPoint\") {\r\n        if (!store.layout?.electrical?.some((p) => p.id === a.id)) continue\r\n        store.removeElectricalPoint(a.id)\r\n      } else if (a.type === \"autoGenerateElectrical\") {\r\n        if (!store.layout) continue\r\n        store.setElectrical(autoGenerateElectrical(store.layou\n\n// editor-store.ts — updateElectricalPoint\nupdateElectricalPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.electrical?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeElectricalPoint: (id) =>\n      commit((l) => {\n        l.electrical = (l.electrical ?? []).filter((p) => p.id !== id);\n      }),\n\n    setElectrical: (points) =>\n      commit((l) => {\n        l.electrical = points;\n      }),\n\n    addWaterPoint: (roomId, type, x, y) =>\n      commit((l) => {\n        const point: WaterPoint = {\n          id: `water-${nanoid(8)}`,\n          roomId,\n          type,\n          x,\n          y,\n        };\n        l.water = [...(l.water ?? []), point];\n      }),\n\n    moveWaterPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateWat\n\n// editor-store.ts — removeElectricalPoint\nremoveElectricalPoint: (id) =>\n      commit((l) => {\n        l.electrical = (l.electrical ?? []).filter((p) => p.id !== id);\n      }),\n\n    setElectrical: (points) =>\n      commit((l) => {\n        l.electrical = points;\n      }),\n\n    addWaterPoint: (roomId, type, x, y) =>\n      commit((l) => {\n        const point: WaterPoint = {\n          id: `water-${nanoid(8)}`,\n          roomId,\n          type,\n          x,\n          y,\n        };\n        l.water = [...(l.water ?? []), point];\n      }),\n\n    moveWaterPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateWaterPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeWaterPoint: ("
 },
 {
  "id": "action:removeElectricalPoint",
  "kind": "action",
  "name": "removeElectricalPoint",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"removeElectricalPoint\"), id: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"removeElectricalPoint\",\"id\":\"<id titik>\"} — hapus titik listrik.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"removeElectricalPoint\") {\r\n        if (!store.layout?.electrical?.some((p) => p.id === a.id)) continue\r\n        store.removeElectricalPoint(a.id)\r\n      } else if (a.type === \"autoGenerateElectrical\") {\r\n        if (!store.layout) continue\r\n        store.setElectrical(autoGenerateElectrical(store.layout, a.floorId))\r\n      } else if (a.type === \"addWaterPoint\") {\r\n        if (!store.layout?.rooms.some((r) => r.id === a.roomId)) continue\r\n        store.addWaterPoint(a.roomId, a.waterT\n\n// editor-store.ts — removeElectricalPoint\nremoveElectricalPoint: (id) =>\n      commit((l) => {\n        l.electrical = (l.electrical ?? []).filter((p) => p.id !== id);\n      }),\n\n    setElectrical: (points) =>\n      commit((l) => {\n        l.electrical = points;\n      }),\n\n    addWaterPoint: (roomId, type, x, y) =>\n      commit((l) => {\n        const point: WaterPoint = {\n          id: `water-${nanoid(8)}`,\n          roomId,\n          type,\n          x,\n          y,\n        };\n        l.water = [...(l.water ?? []), point];\n      }),\n\n    moveWaterPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateWaterPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeWaterPoint: (\n\n// editor-store.ts — setElectrical\nsetElectrical: (points) =>\n      commit((l) => {\n        l.electrical = points;\n      }),\n\n    addWaterPoint: (roomId, type, x, y) =>\n      commit((l) => {\n        const point: WaterPoint = {\n          id: `water-${nanoid(8)}`,\n          roomId,\n          type,\n          x,\n          y,\n        };\n        l.water = [...(l.water ?? []), point];\n      }),\n\n    moveWaterPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateWaterPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeWaterPoint: (id) =>\n      commit((l) => {\n        l.water = (l.water ?? []).filter((p) => p.id !== id);\n      }),\n\n    setWater: (points) =>\n      commit"
 },
 {
  "id": "action:autoGenerateElectrical",
  "kind": "action",
  "name": "autoGenerateElectrical",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"autoGenerateElectrical\"), floorId: z.string().min(1).optional() }),\n\n// dokumentasi prompt\n'- {\"type\":\"autoGenerateElectrical\",\"floorId?\":\"<id lantai>\"} — buat titik listrik default otomatis; tanpa floorId berlaku untuk semua lantai.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"autoGenerateElectrical\") {\r\n        if (!store.layout) continue\r\n        store.setElectrical(autoGenerateElectrical(store.layout, a.floorId))\r\n      } else if (a.type === \"addWaterPoint\") {\r\n        if (!store.layout?.rooms.some((r) => r.id === a.roomId)) continue\r\n        store.addWaterPoint(a.roomId, a.waterType, a.x, a.y)\r\n      } else if (a.type === \"moveWaterPoint\") {\r\n        if (!store.layout?.water?.some((p) => p.id === a.id)) continue\r\n        store.moveWaterPoint(a.id, a.x,\n\n// editor-store.ts — setElectrical\nsetElectrical: (points) =>\n      commit((l) => {\n        l.electrical = points;\n      }),\n\n    addWaterPoint: (roomId, type, x, y) =>\n      commit((l) => {\n        const point: WaterPoint = {\n          id: `water-${nanoid(8)}`,\n          roomId,\n          type,\n          x,\n          y,\n        };\n        l.water = [...(l.water ?? []), point];\n      }),\n\n    moveWaterPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateWaterPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeWaterPoint: (id) =>\n      commit((l) => {\n        l.water = (l.water ?? []).filter((p) => p.id !== id);\n      }),\n\n    setWater: (points) =>\n      commit\n\n// editor-store.ts — addWaterPoint\naddWaterPoint: (roomId, type, x, y) =>\n      commit((l) => {\n        const point: WaterPoint = {\n          id: `water-${nanoid(8)}`,\n          roomId,\n          type,\n          x,\n          y,\n        };\n        l.water = [...(l.water ?? []), point];\n      }),\n\n    moveWaterPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateWaterPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeWaterPoint: (id) =>\n      commit((l) => {\n        l.water = (l.water ?? []).filter((p) => p.id !== id);\n      }),\n\n    setWater: (points) =>\n      commit((l) => {\n        l.water = points;\n      }),\n\n    setSanitation: (patch) =>\n      commit((l) ="
 },
 {
  "id": "action:addWaterPoint",
  "kind": "action",
  "name": "addWaterPoint",
  "evidence": "// skema (actions.ts)\nz.object({\r\n    type: z.literal(\"addWaterPoint\"),\r\n    roomId: z.string().min(1),\r\n    waterType: waterPointTypeSchema,\r\n    x: z.number().finite(),\r\n    y: z.number().finite(),\r\n  }),\n\n// dokumentasi prompt\n'- {\"type\":\"addWaterPoint\",\"roomId\":\"<id>\",\"waterType\":\"kloset|wastafel|shower|kran|kran_taman|floor_drain|sink_dapur|kran_wudhu\",\"x\":<meter>,\"y\":<meter>} — tambah titik air (x/y = koordinat meter ABSOLUT di lahan).\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"addWaterPoint\") {\r\n        if (!store.layout?.rooms.some((r) => r.id === a.roomId)) continue\r\n        store.addWaterPoint(a.roomId, a.waterType, a.x, a.y)\r\n      } else if (a.type === \"moveWaterPoint\") {\r\n        if (!store.layout?.water?.some((p) => p.id === a.id)) continue\r\n        store.moveWaterPoint(a.id, a.x, a.y)\r\n      } else if (a.type === \"updateWaterPoint\") {\r\n        if (!store.layout?.water?.some((p) => p.id === a.id)) continue\r\n        store.updateWaterPoint(a.id, a.pat\n\n// editor-store.ts — addWaterPoint\naddWaterPoint: (roomId, type, x, y) =>\n      commit((l) => {\n        const point: WaterPoint = {\n          id: `water-${nanoid(8)}`,\n          roomId,\n          type,\n          x,\n          y,\n        };\n        l.water = [...(l.water ?? []), point];\n      }),\n\n    moveWaterPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateWaterPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeWaterPoint: (id) =>\n      commit((l) => {\n        l.water = (l.water ?? []).filter((p) => p.id !== id);\n      }),\n\n    setWater: (points) =>\n      commit((l) => {\n        l.water = points;\n      }),\n\n    setSanitation: (patch) =>\n      commit((l) =\n\n// editor-store.ts — moveWaterPoint\nmoveWaterPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateWaterPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeWaterPoint: (id) =>\n      commit((l) => {\n        l.water = (l.water ?? []).filter((p) => p.id !== id);\n      }),\n\n    setWater: (points) =>\n      commit((l) => {\n        l.water = points;\n      }),\n\n    setSanitation: (patch) =>\n      commit((l) => {\n        l.sanitation = { ...l.sanitation, ...patch };\n      }),\n\n    removeSanitationObject: (kind, ref = null) => {\n      commit((l) => {\n        if (!l.sanitation) return;\n        if (kind === \"controlBox\") {\n          const idx = ref ?? -1;\n          l.sanita"
 },
 {
  "id": "action:moveWaterPoint",
  "kind": "action",
  "name": "moveWaterPoint",
  "evidence": "// skema (actions.ts)\nz.object({\r\n    type: z.literal(\"moveWaterPoint\"),\r\n    id: z.string().min(1),\r\n    x: z.number().finite(),\r\n    y: z.number().finite(),\r\n  }),\n\n// dokumentasi prompt\n'- {\"type\":\"moveWaterPoint\",\"id\":\"<id titik>\",\"x\":<meter>,\"y\":<meter>} — pindah titik air ke koordinat absolut.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"moveWaterPoint\") {\r\n        if (!store.layout?.water?.some((p) => p.id === a.id)) continue\r\n        store.moveWaterPoint(a.id, a.x, a.y)\r\n      } else if (a.type === \"updateWaterPoint\") {\r\n        if (!store.layout?.water?.some((p) => p.id === a.id)) continue\r\n        store.updateWaterPoint(a.id, a.patch)\r\n      } else if (a.type === \"removeWaterPoint\") {\r\n        if (!store.layout?.water?.some((p) => p.id === a.id)) continue\r\n        store.removeWaterPoint(a.id)\r\n      } else if (a.\n\n// editor-store.ts — moveWaterPoint\nmoveWaterPoint: (id, x, y) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) {\n          point.x = x;\n          point.y = y;\n        }\n      }),\n\n    updateWaterPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeWaterPoint: (id) =>\n      commit((l) => {\n        l.water = (l.water ?? []).filter((p) => p.id !== id);\n      }),\n\n    setWater: (points) =>\n      commit((l) => {\n        l.water = points;\n      }),\n\n    setSanitation: (patch) =>\n      commit((l) => {\n        l.sanitation = { ...l.sanitation, ...patch };\n      }),\n\n    removeSanitationObject: (kind, ref = null) => {\n      commit((l) => {\n        if (!l.sanitation) return;\n        if (kind === \"controlBox\") {\n          const idx = ref ?? -1;\n          l.sanita\n\n// editor-store.ts — updateWaterPoint\nupdateWaterPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeWaterPoint: (id) =>\n      commit((l) => {\n        l.water = (l.water ?? []).filter((p) => p.id !== id);\n      }),\n\n    setWater: (points) =>\n      commit((l) => {\n        l.water = points;\n      }),\n\n    setSanitation: (patch) =>\n      commit((l) => {\n        l.sanitation = { ...l.sanitation, ...patch };\n      }),\n\n    removeSanitationObject: (kind, ref = null) => {\n      commit((l) => {\n        if (!l.sanitation) return;\n        if (kind === \"controlBox\") {\n          const idx = ref ?? -1;\n          l.sanitation.controlBoxes = (l.sanitation.controlBoxes ?? []).filter(\n            (_, i) => i !== idx,\n          );\n        } else {\n          delete l.sanitation[kind];\n        }\n        if (\n          !l.san"
 },
 {
  "id": "action:updateWaterPoint",
  "kind": "action",
  "name": "updateWaterPoint",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"updateWaterPoint\"), id: z.string().min(1), patch: waterPatchSchema }),\n\n// dokumentasi prompt\n'- {\"type\":\"updateWaterPoint\",\"id\":\"<id titik>\",\"patch\":{type?:\"kloset|...\",\"roomId?\":\"<id>\",\"note?\":\"<teks>\"}} — ubah tipe/ruang/catatan titik air.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"updateWaterPoint\") {\r\n        if (!store.layout?.water?.some((p) => p.id === a.id)) continue\r\n        store.updateWaterPoint(a.id, a.patch)\r\n      } else if (a.type === \"removeWaterPoint\") {\r\n        if (!store.layout?.water?.some((p) => p.id === a.id)) continue\r\n        store.removeWaterPoint(a.id)\r\n      } else if (a.type === \"autoGenerateWater\") {\r\n        if (!store.layout) continue\r\n        store.setWater(autoGenerateWater(store.layout, a.floorId))\r\n      } else if (a.type === \"\n\n// editor-store.ts — updateWaterPoint\nupdateWaterPoint: (id, patch) =>\n      commit((l) => {\n        const point = l.water?.find((p) => p.id === id);\n        if (point) Object.assign(point, patch);\n      }),\n\n    removeWaterPoint: (id) =>\n      commit((l) => {\n        l.water = (l.water ?? []).filter((p) => p.id !== id);\n      }),\n\n    setWater: (points) =>\n      commit((l) => {\n        l.water = points;\n      }),\n\n    setSanitation: (patch) =>\n      commit((l) => {\n        l.sanitation = { ...l.sanitation, ...patch };\n      }),\n\n    removeSanitationObject: (kind, ref = null) => {\n      commit((l) => {\n        if (!l.sanitation) return;\n        if (kind === \"controlBox\") {\n          const idx = ref ?? -1;\n          l.sanitation.controlBoxes = (l.sanitation.controlBoxes ?? []).filter(\n            (_, i) => i !== idx,\n          );\n        } else {\n          delete l.sanitation[kind];\n        }\n        if (\n          !l.san\n\n// editor-store.ts — removeWaterPoint\nremoveWaterPoint: (id) =>\n      commit((l) => {\n        l.water = (l.water ?? []).filter((p) => p.id !== id);\n      }),\n\n    setWater: (points) =>\n      commit((l) => {\n        l.water = points;\n      }),\n\n    setSanitation: (patch) =>\n      commit((l) => {\n        l.sanitation = { ...l.sanitation, ...patch };\n      }),\n\n    removeSanitationObject: (kind, ref = null) => {\n      commit((l) => {\n        if (!l.sanitation) return;\n        if (kind === \"controlBox\") {\n          const idx = ref ?? -1;\n          l.sanitation.controlBoxes = (l.sanitation.controlBoxes ?? []).filter(\n            (_, i) => i !== idx,\n          );\n        } else {\n          delete l.sanitation[kind];\n        }\n        if (\n          !l.sanitation.septicTank &&\n          !l.sanitation.soakwell &&\n          (l.sanitation.controlBoxes?.length ?? 0) === 0\n        ) {\n          delete l.sanitation;\n        }\n      }"
 },
 {
  "id": "action:removeWaterPoint",
  "kind": "action",
  "name": "removeWaterPoint",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"removeWaterPoint\"), id: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"removeWaterPoint\",\"id\":\"<id titik>\"} — hapus titik air.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"removeWaterPoint\") {\r\n        if (!store.layout?.water?.some((p) => p.id === a.id)) continue\r\n        store.removeWaterPoint(a.id)\r\n      } else if (a.type === \"autoGenerateWater\") {\r\n        if (!store.layout) continue\r\n        store.setWater(autoGenerateWater(store.layout, a.floorId))\r\n      } else if (a.type === \"autoSizeSanitation\") {\r\n        if (!store.layout || !store.site) continue\r\n        store.setSanitation(\r\n          autoSizeSanitation(store.layout, roofAreaForLayout(sto\n\n// editor-store.ts — removeWaterPoint\nremoveWaterPoint: (id) =>\n      commit((l) => {\n        l.water = (l.water ?? []).filter((p) => p.id !== id);\n      }),\n\n    setWater: (points) =>\n      commit((l) => {\n        l.water = points;\n      }),\n\n    setSanitation: (patch) =>\n      commit((l) => {\n        l.sanitation = { ...l.sanitation, ...patch };\n      }),\n\n    removeSanitationObject: (kind, ref = null) => {\n      commit((l) => {\n        if (!l.sanitation) return;\n        if (kind === \"controlBox\") {\n          const idx = ref ?? -1;\n          l.sanitation.controlBoxes = (l.sanitation.controlBoxes ?? []).filter(\n            (_, i) => i !== idx,\n          );\n        } else {\n          delete l.sanitation[kind];\n        }\n        if (\n          !l.sanitation.septicTank &&\n          !l.sanitation.soakwell &&\n          (l.sanitation.controlBoxes?.length ?? 0) === 0\n        ) {\n          delete l.sanitation;\n        }\n      }\n\n// editor-store.ts — setWater\nsetWater: (points) =>\n      commit((l) => {\n        l.water = points;\n      }),\n\n    setSanitation: (patch) =>\n      commit((l) => {\n        l.sanitation = { ...l.sanitation, ...patch };\n      }),\n\n    removeSanitationObject: (kind, ref = null) => {\n      commit((l) => {\n        if (!l.sanitation) return;\n        if (kind === \"controlBox\") {\n          const idx = ref ?? -1;\n          l.sanitation.controlBoxes = (l.sanitation.controlBoxes ?? []).filter(\n            (_, i) => i !== idx,\n          );\n        } else {\n          delete l.sanitation[kind];\n        }\n        if (\n          !l.sanitation.septicTank &&\n          !l.sanitation.soakwell &&\n          (l.sanitation.controlBoxes?.length ?? 0) === 0\n        ) {\n          delete l.sanitation;\n        }\n      });\n      set({ selectedObjectId: null });\n    },\n\n    beginDrag: () => {\n      const { layout } = get();\n      set({"
 },
 {
  "id": "action:autoGenerateWater",
  "kind": "action",
  "name": "autoGenerateWater",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"autoGenerateWater\"), floorId: z.string().min(1).optional() }),\n\n// dokumentasi prompt\n'- {\"type\":\"autoGenerateWater\",\"floorId?\":\"<id lantai>\"} — buat titik air default otomatis; tanpa floorId berlaku untuk semua lantai.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"autoGenerateWater\") {\r\n        if (!store.layout) continue\r\n        store.setWater(autoGenerateWater(store.layout, a.floorId))\r\n      } else if (a.type === \"autoSizeSanitation\") {\r\n        if (!store.layout || !store.site) continue\r\n        store.setSanitation(\r\n          autoSizeSanitation(store.layout, roofAreaForLayout(store.layout), store.site)\r\n        )\r\n      } else if (a.type === \"moveSanitationObject\") {\r\n        const san = store.layout?.sanitation\r\n        const exists =\n\n// editor-store.ts — setWater\nsetWater: (points) =>\n      commit((l) => {\n        l.water = points;\n      }),\n\n    setSanitation: (patch) =>\n      commit((l) => {\n        l.sanitation = { ...l.sanitation, ...patch };\n      }),\n\n    removeSanitationObject: (kind, ref = null) => {\n      commit((l) => {\n        if (!l.sanitation) return;\n        if (kind === \"controlBox\") {\n          const idx = ref ?? -1;\n          l.sanitation.controlBoxes = (l.sanitation.controlBoxes ?? []).filter(\n            (_, i) => i !== idx,\n          );\n        } else {\n          delete l.sanitation[kind];\n        }\n        if (\n          !l.sanitation.septicTank &&\n          !l.sanitation.soakwell &&\n          (l.sanitation.controlBoxes?.length ?? 0) === 0\n        ) {\n          delete l.sanitation;\n        }\n      });\n      set({ selectedObjectId: null });\n    },\n\n    beginDrag: () => {\n      const { layout } = get();\n      set({\n\n// editor-store.ts — setSanitation\nsetSanitation: (patch) =>\n      commit((l) => {\n        l.sanitation = { ...l.sanitation, ...patch };\n      }),\n\n    removeSanitationObject: (kind, ref = null) => {\n      commit((l) => {\n        if (!l.sanitation) return;\n        if (kind === \"controlBox\") {\n          const idx = ref ?? -1;\n          l.sanitation.controlBoxes = (l.sanitation.controlBoxes ?? []).filter(\n            (_, i) => i !== idx,\n          );\n        } else {\n          delete l.sanitation[kind];\n        }\n        if (\n          !l.sanitation.septicTank &&\n          !l.sanitation.soakwell &&\n          (l.sanitation.controlBoxes?.length ?? 0) === 0\n        ) {\n          delete l.sanitation;\n        }\n      });\n      set({ selectedObjectId: null });\n    },\n\n    beginDrag: () => {\n      const { layout } = get();\n      set({\n        _dragBase: layout ? clone(layout) : null,\n        _dragPushed: false,\n        alignme"
 },
 {
  "id": "action:moveSanitationObject",
  "kind": "action",
  "name": "moveSanitationObject",
  "evidence": "// skema (actions.ts)\nz.object({\r\n    type: z.literal(\"moveSanitationObject\"),\r\n    kind: sanitationKindSchema,\r\n    ref: z.number().int().nonnegative().optional(),\r\n    x: z.number().finite(),\r\n    y: z.number().finite(),\r\n  }),\n\n// dokumentasi prompt\n'- {\"type\":\"moveSanitationObject\",\"kind\":\"septicTank|soakwell|controlBox\",\"ref?\":<indeks bak kontrol>,\"x\":<meter>,\"y\":<meter>} — geser objek sanitasi di LAHAN (x/y = koordinat meter absolut pada lahan; ref hanya untuk controlBox = indeks bak).\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"moveSanitationObject\") {\r\n        const san = store.layout?.sanitation\r\n        const exists =\r\n          a.kind === \"controlBox\"\r\n            ? Boolean(san?.controlBoxes?.[a.ref ?? -1])\r\n            : Boolean(san?.[a.kind])\r\n        if (!exists) continue\r\n        // moveSanitationObject is live()-based (drag gesture); bracket it so a\r\n        // single AI-dispatched move records exactly one undo entry (parity with\r\n        // SP4's commit-based moveElectricalPoint).\r\n        store.b"
 },
 {
  "id": "action:autoSizeSanitation",
  "kind": "action",
  "name": "autoSizeSanitation",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"autoSizeSanitation\") }),\n\n// dokumentasi prompt\n'- {\"type\":\"autoSizeSanitation\"} — hitung ulang & tempatkan septic tank, sumur resapan, dan bak kontrol berukuran SNI (level lahan; tanpa argumen).\\n\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"autoSizeSanitation\") {\r\n        if (!store.layout || !store.site) continue\r\n        store.setSanitation(\r\n          autoSizeSanitation(store.layout, roofAreaForLayout(store.layout), store.site)\r\n        )\r\n      } else if (a.type === \"moveSanitationObject\") {\r\n        const san = store.layout?.sanitation\r\n        const exists =\r\n          a.kind === \"controlBox\"\r\n            ? Boolean(san?.controlBoxes?.[a.ref ?? -1])\r\n            : Boolean(san?.[a.kind])\r\n        if (!exists) contin\n\n// editor-store.ts — setSanitation\nsetSanitation: (patch) =>\n      commit((l) => {\n        l.sanitation = { ...l.sanitation, ...patch };\n      }),\n\n    removeSanitationObject: (kind, ref = null) => {\n      commit((l) => {\n        if (!l.sanitation) return;\n        if (kind === \"controlBox\") {\n          const idx = ref ?? -1;\n          l.sanitation.controlBoxes = (l.sanitation.controlBoxes ?? []).filter(\n            (_, i) => i !== idx,\n          );\n        } else {\n          delete l.sanitation[kind];\n        }\n        if (\n          !l.sanitation.septicTank &&\n          !l.sanitation.soakwell &&\n          (l.sanitation.controlBoxes?.length ?? 0) === 0\n        ) {\n          delete l.sanitation;\n        }\n      });\n      set({ selectedObjectId: null });\n    },\n\n    beginDrag: () => {\n      const { layout } = get();\n      set({\n        _dragBase: layout ? clone(layout) : null,\n        _dragPushed: false,\n        alignme"
 },
 {
  "id": "action:addFurniture",
  "kind": "action",
  "name": "addFurniture",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"addFurniture\"), roomId: z.string().min(1), furnitureId: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"addFurniture\",\"roomId\":\"<id>\",\"furnitureId\":\"<id katalog>\"}.\\n' +\n\n// penerapan sebenarnya (apply.ts)\nif (a.type === \"addFurniture\") {\r\n        const item = getFurniture(a.furnitureId)\r\n        if (!item) continue\r\n        const result = store.addFurniture(a.roomId, item)\r\n        if (!result.ok) continue\r\n      } else if (a.type === \"moveFurniture\") {\r\n        if (!roomPlan.furniture.some((item) => item.id === a.furnitureId)) continue\r\n        store.moveFurniture(a.roomId, a.furnitureId, a.x, a.y)\r\n      } else if (a.type === \"rotateFurniture\") {\r\n        if (!roomPlan.furniture.some((item) => item"
 },
 {
  "id": "action:moveFurniture",
  "kind": "action",
  "name": "moveFurniture",
  "evidence": "// skema (actions.ts)\nz.object({\r\n    type: z.literal(\"moveFurniture\"),\r\n    roomId: z.string().min(1),\r\n    furnitureId: z.string().min(1),\r\n    x: z.number().finite(),\r\n    y: z.number().finite(),\r\n  }),\n\n// dokumentasi prompt\n'- {\"type\":\"moveFurniture\",\"roomId\":\"<id>\",\"furnitureId\":\"<id furnitur di ruang>\",\"x\":<meter>,\"y\":<meter>}.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"moveFurniture\") {\r\n        if (!roomPlan.furniture.some((item) => item.id === a.furnitureId)) continue\r\n        store.moveFurniture(a.roomId, a.furnitureId, a.x, a.y)\r\n      } else if (a.type === \"rotateFurniture\") {\r\n        if (!roomPlan.furniture.some((item) => item.id === a.furnitureId)) continue\r\n        store.rotateFurniture(a.roomId, a.furnitureId)\r\n      } else if (a.type === \"removeFurniture\") {\r\n        if (!roomPlan.furniture.some((item) => item.id === a.furnitureId)) cont"
 },
 {
  "id": "action:rotateFurniture",
  "kind": "action",
  "name": "rotateFurniture",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"rotateFurniture\"), roomId: z.string().min(1), furnitureId: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"rotateFurniture\",\"roomId\":\"<id>\",\"furnitureId\":\"<id furnitur>\"} — putar 90°.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"rotateFurniture\") {\r\n        if (!roomPlan.furniture.some((item) => item.id === a.furnitureId)) continue\r\n        store.rotateFurniture(a.roomId, a.furnitureId)\r\n      } else if (a.type === \"removeFurniture\") {\r\n        if (!roomPlan.furniture.some((item) => item.id === a.furnitureId)) continue\r\n        store.removeFurniture(a.roomId, a.furnitureId)\r\n      } else if (a.type === \"resetRoom\") {\r\n        store.resetRoom(a.roomId)\r\n      } else if (a.type === \"addLight\") {\r\n        store"
 },
 {
  "id": "action:removeFurniture",
  "kind": "action",
  "name": "removeFurniture",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"removeFurniture\"), roomId: z.string().min(1), furnitureId: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"removeFurniture\",\"roomId\":\"<id>\",\"furnitureId\":\"<id furnitur>\"}.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"removeFurniture\") {\r\n        if (!roomPlan.furniture.some((item) => item.id === a.furnitureId)) continue\r\n        store.removeFurniture(a.roomId, a.furnitureId)\r\n      } else if (a.type === \"resetRoom\") {\r\n        store.resetRoom(a.roomId)\r\n      } else if (a.type === \"addLight\") {\r\n        store.addLight(a.roomId, a.lightType)\r\n      } else if (a.type === \"moveLight\") {\r\n        if (!roomPlan.lighting.some((light) => light.id === a.lightId)) continue\r\n        store.moveLight(a.roomI"
 },
 {
  "id": "action:setStyle",
  "kind": "action",
  "name": "setStyle",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"setStyle\"), style: interiorStyleSchema }),\n\n// dokumentasi prompt\n'- {\"type\":\"setStyle\",\"style\":\"<id gaya>\"}.\\n' +\n\n// penerapan sebenarnya (apply.ts)\nif (a.type === \"setStyle\") {\r\n        store.setStyle(a.style)\r\n        applied++\r\n        continue\r\n      }\r\n      if (!store.plan?.rooms.some((r) => r.roomId === a.roomId)) continue\r\n      const roomPlan = store.plan.rooms.find((r) => r.roomId === a.roomId)!\r\n      if (a.type === \"addFurniture\") {\r\n        const item = getFurniture(a.furnitureId)\r\n        if (!item) continue\r\n        const result = store.addFurniture(a.roomId, item)\r\n        if (!result.ok) continue\r\n      } else if (a.type === \"mo"
 },
 {
  "id": "action:resetRoom",
  "kind": "action",
  "name": "resetRoom",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"resetRoom\"), roomId: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"resetRoom\",\"roomId\":\"<id>\"} — tata ulang otomatis seluruh ruang.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"resetRoom\") {\r\n        store.resetRoom(a.roomId)\r\n      } else if (a.type === \"addLight\") {\r\n        store.addLight(a.roomId, a.lightType)\r\n      } else if (a.type === \"moveLight\") {\r\n        if (!roomPlan.lighting.some((light) => light.id === a.lightId)) continue\r\n        store.moveLight(a.roomId, a.lightId, a.x, a.y)\r\n      } else if (a.type === \"updateLight\") {\r\n        if (!roomPlan.lighting.some((light) => light.id === a.lightId)) continue\r\n        const patch: Parameters<typeof"
 },
 {
  "id": "action:addLight",
  "kind": "action",
  "name": "addLight",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"addLight\"), roomId: z.string().min(1), lightType: lightTypeSchema }),\n\n// dokumentasi prompt\n'- {\"type\":\"addLight\",\"roomId\":\"<id>\",\"lightType\":\"downlight|pendant|wall_lamp|indirect|task|outdoor\"} — tambah lampu baru.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"addLight\") {\r\n        store.addLight(a.roomId, a.lightType)\r\n      } else if (a.type === \"moveLight\") {\r\n        if (!roomPlan.lighting.some((light) => light.id === a.lightId)) continue\r\n        store.moveLight(a.roomId, a.lightId, a.x, a.y)\r\n      } else if (a.type === \"updateLight\") {\r\n        if (!roomPlan.lighting.some((light) => light.id === a.lightId)) continue\r\n        const patch: Parameters<typeof store.updateLight>[2] = {}\r\n        if (a.patch.lightType != null) patch.type"
 },
 {
  "id": "action:moveLight",
  "kind": "action",
  "name": "moveLight",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"moveLight\"), roomId: z.string().min(1), lightId: z.string().min(1), x: z.number().finite(), y: z.number().finite() }),\n\n// dokumentasi prompt\n'- {\"type\":\"moveLight\",\"roomId\":\"<id>\",\"lightId\":\"<id lampu di ruang>\",\"x\":<meter>,\"y\":<meter>} — geser posisi lampu.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"moveLight\") {\r\n        if (!roomPlan.lighting.some((light) => light.id === a.lightId)) continue\r\n        store.moveLight(a.roomId, a.lightId, a.x, a.y)\r\n      } else if (a.type === \"updateLight\") {\r\n        if (!roomPlan.lighting.some((light) => light.id === a.lightId)) continue\r\n        const patch: Parameters<typeof store.updateLight>[2] = {}\r\n        if (a.patch.lightType != null) patch.type = a.patch.lightType\r\n        if (a.patch.colorTemperature != null) patch.colorTemperature"
 },
 {
  "id": "action:updateLight",
  "kind": "action",
  "name": "updateLight",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"updateLight\"), roomId: z.string().min(1), lightId: z.string().min(1),\r\n    patch: z.object({ lightType: lightTypeSchema.optional(), colorTemperature: colorTempSchema.optional(), qty: z.number().int().positive().optional(), heightM: z.number().finite().positive().optional(), watt: z.number().finite().positive().optional() }) }),\n\n// dokumentasi prompt\n'- {\"type\":\"updateLight\",\"roomId\":\"<id>\",\"lightId\":\"<id lampu>\",\"patch\":{\"lightType\"?:\"...\",\"colorTemperature\"?:\"warm|neutral|cool\",\"qty\"?:<int>,\"heightM\"?:<meter>,\"watt\"?:<1-200, daya per unit — dipakai sirkuit/RAB/estimasi energi>}} — ubah properti lampu.\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"updateLight\") {\r\n        if (!roomPlan.lighting.some((light) => light.id === a.lightId)) continue\r\n        const patch: Parameters<typeof store.updateLight>[2] = {}\r\n        if (a.patch.lightType != null) patch.type = a.patch.lightType\r\n        if (a.patch.colorTemperature != null) patch.colorTemperature = a.patch.colorTemperature\r\n        if (a.patch.qty != null) patch.qty = a.patch.qty\r\n        if (a.patch.heightM != null) patch.heightM = a.patch.heightM\r\n        if (a.patch.watt !"
 },
 {
  "id": "action:removeLight",
  "kind": "action",
  "name": "removeLight",
  "evidence": "// skema (actions.ts)\nz.object({ type: z.literal(\"removeLight\"), roomId: z.string().min(1), lightId: z.string().min(1) }),\n\n// dokumentasi prompt\n'- {\"type\":\"removeLight\",\"roomId\":\"<id>\",\"lightId\":\"<id lampu>\"} — hapus lampu.\\n\\n' +\n\n// penerapan sebenarnya (apply.ts)\n} else if (a.type === \"removeLight\") {\r\n        if (!roomPlan.lighting.some((light) => light.id === a.lightId)) continue\r\n        store.removeLight(a.roomId, a.lightId)\r\n      }\r\n      applied++\r\n    } catch {\r\n      // skip a failing action; keep applying the rest\r\n    }\r\n  }\r\n  return applied\r\n}\r\n\r\n/**\r\n * Apply a proposal as one conceptual transaction. Any stale/invalid action\r\n * rolls the whole store back; a successful batch is collapsed to one Undo.\r\n */\r\nexport function applyFloorplanActionsAtomic(a"
 },
 {
  "id": "room_type:kamar_tidur",
  "kind": "room_type",
  "name": "kamar_tidur",
  "evidence": "// ROOM_TYPES (constants)\nkamar_tidur: { label: \"Kamar tidur\", defaultAreaM2: 12 }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:kamar_mandi",
  "kind": "room_type",
  "name": "kamar_mandi",
  "evidence": "// ROOM_TYPES (constants)\nkamar_mandi: { label: \"Kamar mandi\", defaultAreaM2: 4 }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:ruang_tamu",
  "kind": "room_type",
  "name": "ruang_tamu",
  "evidence": "// ROOM_TYPES (constants)\nruang_tamu: { label: \"Ruang tamu\", defaultAreaM2: 14 }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:ruang_keluarga",
  "kind": "room_type",
  "name": "ruang_keluarga",
  "evidence": "// ROOM_TYPES (constants)\nruang_keluarga: { label: \"Ruang keluarga\", defaultAreaM2: 16 }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:dapur",
  "kind": "room_type",
  "name": "dapur",
  "evidence": "// ROOM_TYPES (constants)\ndapur: { label: \"Dapur\", defaultAreaM2: 9 }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:ruang_makan",
  "kind": "room_type",
  "name": "ruang_makan",
  "evidence": "// ROOM_TYPES (constants)\nruang_makan: { label: \"Ruang makan\", defaultAreaM2: 10 }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:musholla",
  "kind": "room_type",
  "name": "musholla",
  "evidence": "// ROOM_TYPES (constants)\nmusholla: { label: \"Musholla\", defaultAreaM2: 6 }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:laundry",
  "kind": "room_type",
  "name": "laundry",
  "evidence": "// ROOM_TYPES (constants)\nlaundry: { label: \"Laundry\", defaultAreaM2: 4 }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:gudang",
  "kind": "room_type",
  "name": "gudang",
  "evidence": "// ROOM_TYPES (constants)\ngudang: { label: \"Gudang\", defaultAreaM2: 4 }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:balkon",
  "kind": "room_type",
  "name": "balkon",
  "evidence": "// ROOM_TYPES (constants)\nbalkon: { label: \"Balkon\", defaultAreaM2: 6, outdoor: true }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:rooftop_lounge",
  "kind": "room_type",
  "name": "rooftop_lounge",
  "evidence": "// ROOM_TYPES (constants)\nrooftop_lounge: { label: \"Rooftop lounge\", defaultAreaM2: 18, outdoor: true }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:area_kumpul",
  "kind": "room_type",
  "name": "area_kumpul",
  "evidence": "// ROOM_TYPES (constants)\narea_kumpul: { label: \"Area kumpul keluarga\", defaultAreaM2: 20 }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:kolam",
  "kind": "room_type",
  "name": "kolam",
  "evidence": "// ROOM_TYPES (constants)\nkolam: { label: \"Kolam\", defaultAreaM2: 12, outdoor: true }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:taman",
  "kind": "room_type",
  "name": "taman",
  "evidence": "// ROOM_TYPES (constants)\ntaman: { label: \"Taman\", defaultAreaM2: 10, outdoor: true }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:workspace",
  "kind": "room_type",
  "name": "workspace",
  "evidence": "// ROOM_TYPES (constants)\nworkspace: { label: \"Workspace\", defaultAreaM2: 8 }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:carport",
  "kind": "room_type",
  "name": "carport",
  "evidence": "// ROOM_TYPES (constants)\ncarport: { label: \"Carport\", defaultAreaM2: 15, outdoor: true }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:void",
  "kind": "room_type",
  "name": "void",
  "evidence": "// ROOM_TYPES (constants)\nvoid: { label: \"Void\", defaultAreaM2: 6 }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "room_type:tangga",
  "kind": "room_type",
  "name": "tangga",
  "evidence": "// ROOM_TYPES (constants)\ntangga: { label: \"Tangga\", defaultAreaM2: 6 }\n\n// Ruang bertipe di OPEN_TYPES dirender TANPA dinding (build-model.ts: if (!isOpen) …)\nexport const OPEN_TYPES: RoomType[]"
 },
 {
  "id": "mechanic:dinding-otomatis",
  "name": "Dinding otomatis dari persegi ruang",
  "evidence": "// build-model.ts — dinding TIDAK disimpan di data; diturunkan saat render\nif (!isOpen) {\n        for (const side of SIDES) {\n          const full: Span = { start: 0, end: side === \"n\" || side === \"s\" ? w : d }\n          const horizontal = side === \"n\" || side === \"s\"\n          const line =\n            side === \"n\" ? room.y\n            : side === \"s\" ? room.y + d\n            : side === \"w\" ? room.x\n            : room.x + w\n          // De-dupe shared interior walls: every room drawing its own four\n          // sides puts TWO coincident boxes on a shared boundary — coplanar\n          // faces z-fight (flickering seams at room junctions). Ownership\n          // rule: the wall on a shared line belongs to the room on its\n          // NORTH/WEST side (that room's s/e wa\n\n// OPEN_TYPES = dirender tanpa dinding\nexport const OPEN_TYPES: RoomType[]",
  "kind": "mechanic"
 },
 {
  "id": "mechanic:wallid-dua-sisi",
  "name": "Bukaan di dinding bersama melayani dua ruang",
  "evidence": "// geometry: openingServesRoom — pintu terdaftar di SATU wallId tapi melayani kedua sisi\nexport function openingServesRoom(\n  opening: Pick<Opening, \"wallId\" | \"positionM\" | \"widthM\" | \"floorId\">,\n  room: Room,\n  allRooms: Room[],\n  tol = 0.12\n): boolean {\n  const parsed = parseOpeningWall(opening.wallId)\n  if (!parsed) return false\n  if (parsed.roomId === room.id) return true\n  if (opening.floorId !== room.floorId) return false\n  const host = allRooms.find((r) => r.id === parsed.roomId)\n  if (!host || host.floorId !== room.floorId) return false\n  const seg = openingSegment(host, parsed.side, opening.positionM, opening.widthM)\n  const MIN_SHARED = 0.1\n  if (parsed.side === \"n\" || parsed.side === \"s\") {\n    const onBoundary =\n      Math.abs(seg.y1 - room.y) <= tol || Math.abs(seg.y1 - (room.y + room.depth)) <= tol\n    const overlap = Math.min(seg.x2, room.x + room.width) - Math.max(seg.x1, room.x)\n    return onBoundary && overlap > MIN_SHARED\n  }\n  const onBoundary =\n    Math",
  "kind": "mechanic"
 },
 {
  "id": "mechanic:positionm",
  "name": "positionM = titik tengah bukaan pada dinding",
  "evidence": "// geometry: openingSegment\nexport function openingSegment(\n  room: Room,\n  side: Side,\n  positionM: number,\n  widthM: number\n): { x1: number; y1: number; x2: number; y2: number } {\n  const len = edgeLength(room, side)\n  const half = widthM / 2\n  const center = clamp(positionM, half, Math.max(half, len - half))\n  switch (side) {\n    case \"n\":\n      return { x1: room.x + center - half, y1: room.y, x2: room.x + center + half, y2: room.y }\n    case \"s\":\n      return {\n        x1: room.x + center - half,\n        y1: room.y + room.depth,\n        x2: room.x + center + half,\n        y2: room.y + room.depth,\n      }\n    case \"w\":\n      return { x1: room.x, y1: room.y + center - half, x2: room.x, y2: room.y + center + half }\n    case \"e\":\n    default:\n      return {\n        x1: room.x + room.width,\n        y1: room.y + center - half,\n        x2: room.x + room.width,\n        y2: room.y + center + half,\n      }\n  }\n}\n\n/** Nea",
  "kind": "mechanic"
 },
 {
  "id": "mechanic:konektivitas",
  "name": "Keterjangkauan ruang lewat graf pintu",
  "evidence": "// geometry/connectivity.ts\nexport function analyzeRoomConnectivity(rooms: Room[], openings: Opening[]): RoomConnectivity {\n  const indoor = rooms.filter((r) => !isOutdoorRoom(r))\n  const adj = new Map<string, string[]>(indoor.map((r) => [r.id, []]))\n  const withExteriorDoor = new Set<string>()\n\n  for (const o of openings) {\n    if (o.type !== \"door\") continue\n    const parsed = parseOpeningWall(o.wallId)\n    if (!parsed) continue\n    const host = rooms.find((r) => r.id === parsed.roomId)\n    if (!host) continue\n    const sameFloor = rooms.filter((r) => r.floorId === host.floorId && r.id !== host.id)\n    const neighbor = roomsAdjacentOnSide(host, parsed.side, sameFloor, ADJACENCY_TOL)\n    const hostIndoor = !isOutdoorRoom(host)\n    const neighborIndoor = !!neighbor && !isOutdoorRoom(neighbor)\n\n    if (hostIndoor && neighborIndoor) {\n      adj.get(host.id)?.push(neighbor.id)\n      adj.get(neighbor.id)?.push(host.id)\n    } else if (hostIndoor) {\n      withExteriorDoor.add(host.id) // ke luar atau ke ruang terbuka\n    } else if (neighborIndoor) {\n      withExteriorDoor.add(neighbor.id)\n    }\n  }\n\n  const componentOf = new Map<string, number>()\n  let comp = 0\n  for (const room of indoor) {\n    if (componentOf.has",
  "kind": "mechanic"
 },
 {
  "id": "mechanic:akses-vs-ventilasi",
  "name": "Akses (pintu) berbeda dari ventilasi (bukaan apa pun)",
  "evidence": "// validation.ts — aturan lama hanya menuntut BUKAAN utk ventilasi\nrequiresVentilation) {\n      const hasOpening = openings.some((o) => openingServesRoom(o, room, rooms))\n      if (!hasOpening) {\n        issues.push({\n          id: `vent:${room.id}`,\n          level: \"warning\",\n          category: \"spatial\",\n          message: `${room.name} belum punya jendela/pintu untuk ventilasi.`,\n          objectId: room.id,\n        })\n      }\n    }\n\n    // Rooftop room outside the deck area (partial rooftop only).\n    if (deckRect && room.floorId === \"floor-rooftop\") {\n      const outside =\n        room.x < deckRect.x - 0.01 ||\n        room.y < deckRect.y - 0.01 ||\n    \n\n// design-audit.ts — aturan akses & keterputusan\nfunction auditRoomAccess(layout: DesignLayout): AuditFinding[] {\n  const openings = Array.isArray(layout.openings) ? layout.openings : []\n  const rooms = Array.isArray(layout.rooms) ? layout.rooms : []\n  const doors = openings.filter((o) => o.type === \"door\")\n  const out: AuditFinding[] = []\n\n  // (1) Tidak punya pintu sama sekali.\n  const doorless = new Set<string>()\n  for (const room of rooms) {\n    // Ruang terbuka (carport/taman/teras/balkon/void…) memang tanpa daun pintu.\n    if (OPEN_TYPES.includes(room.type)) continue\n    if (doors.some((d) => openingServesRoom(d, room, rooms))) continue\n    doorless.add(room.id)\n    out.push({\n      id: `sirkulasi-akses:${room.id}`,\n      category: \"sirkulasi\",\n      severity: \"warning\",\n      title: `${room.name} belum punya pintu`,\n      detail:\n        `${room.name} tertutup dinding tanpa pintu, jadi belum bisa diakses. ` +\n        `Jendela saja tidak cukup — itu hanya memenuhi kebutuhan ventilasi.`,\n      standard: \"Aksesibilitas ruang\",\n      fix: `Tambahkan pintu pada salah satu dinding ${room.name} yang berbatasan dengan ruang sirkulasi.`,\n      objectId: room.id,\n    })\n  }\n\n  // (2) Punya pintu, TAPI terputus dari inti rumah. Memeriksa keberadaan\n  //     pintu saja tak cukup: sebuah kamar bisa punya pintu ke halaman dan\n  //     pintu ke kamar mandinya sendiri, dan tetap tak terjangkau dari ruang\n  //     keluarga — persis kel",
  "kind": "mechanic"
 },
 {
  "id": "element:fixed_window",
  "kind": "element",
  "name": "fixed_window",
  "evidence": "// OPENING_KIND_META\nfixed_window: {\n    label: \"Fixed window\",\n    type: \"window\",\n    purpose: \"vision\",\n    operation: \"fixed\",\n    defaultWidthM: 1.2,\n    defaultHeightM: 1.2,\n    defaultSillHeightM: 0.8,\n    frameMaterial: \"aluminium\",\n    privacyLevel: \"medium\",\n    shading: \"overhang\",\n}"
 },
 {
  "id": "element:casement_window",
  "kind": "element",
  "name": "casement_window",
  "evidence": "// OPENING_KIND_META\ncasement_window: {\n    label: \"Casement window\",\n    type: \"window\",\n    purpose: \"ventilation\",\n    operation: \"swing\",\n    defaultWidthM: 0.8,\n    defaultHeightM: 1.2,\n    defaultSillHeightM: 0.8,\n    frameMaterial: \"aluminium\",\n    privacyLevel: \"medium\",\n    shading: \"overhang\",\n}"
 },
 {
  "id": "element:sliding_window",
  "kind": "element",
  "name": "sliding_window",
  "evidence": "// OPENING_KIND_META\nsliding_window: {\n    label: \"Sliding window\",\n    type: \"window\",\n    purpose: \"ventilation\",\n    operation: \"sliding\",\n    defaultWidthM: 1.2,\n    defaultHeightM: 1.2,\n    defaultSillHeightM: 0.8,\n    frameMaterial: \"aluminium\",\n    privacyLevel: \"medium\",\n    shading: \"overhang\",\n}"
 },
 {
  "id": "element:awning_window",
  "kind": "element",
  "name": "awning_window",
  "evidence": "// OPENING_KIND_META\nawning_window: {\n    label: \"Awning window\",\n    type: \"window\",\n    purpose: \"ventilation\",\n    operation: \"swing\",\n    defaultWidthM: 0.8,\n    defaultHeightM: 0.8,\n    defaultSillHeightM: 1,\n    frameMaterial: \"aluminium\",\n    privacyLevel: \"medium\",\n    shading: \"none\",\n}"
 },
 {
  "id": "element:clerestory_window",
  "kind": "element",
  "name": "clerestory_window",
  "evidence": "// OPENING_KIND_META\nclerestory_window: {\n    label: \"Clerestory window\",\n    type: \"window\",\n    purpose: \"vision\",\n    operation: \"fixed\",\n    defaultWidthM: 1.2,\n    defaultHeightM: 0.5,\n    defaultSillHeightM: 2.1,\n    frameMaterial: \"aluminium\",\n    privacyLevel: \"high\",\n    shading: \"none\",\n}"
 },
 {
  "id": "element:curtain_wall",
  "kind": "element",
  "name": "curtain_wall",
  "evidence": "// OPENING_KIND_META\ncurtain_wall: {\n    label: \"Curtain wall / kaca penuh\",\n    type: \"window\",\n    purpose: \"vision\",\n    operation: \"fixed\",\n    defaultWidthM: 2.4,\n    defaultHeightM: 2.7,\n    defaultSillHeightM: 0,\n    frameMaterial: \"frameless\",\n    privacyLevel: \"low\",\n    shading: \"secondary_skin\",\n}"
 },
 {
  "id": "element:skylight",
  "kind": "element",
  "name": "skylight",
  "evidence": "// OPENING_KIND_META\nskylight: {\n    label: \"Void / skylight\",\n    type: \"window\",\n    purpose: \"vision\",\n    operation: \"roof\",\n    defaultWidthM: 1.2,\n    defaultHeightM: 1.2,\n    defaultSillHeightM: 2.6,\n    frameMaterial: \"aluminium\",\n    privacyLevel: \"high\",\n    shading: \"none\",\n}"
 },
 {
  "id": "element:jalousie_window",
  "kind": "element",
  "name": "jalousie_window",
  "evidence": "// OPENING_KIND_META\njalousie_window: {\n    label: \"Jalousie / louvre window\",\n    type: \"window\",\n    purpose: \"ventilation\",\n    operation: \"louvre\",\n    defaultWidthM: 0.8,\n    defaultHeightM: 1.2,\n    defaultSillHeightM: 0.8,\n    frameMaterial: \"aluminium\",\n    privacyLevel: \"high\",\n    shading: \"overhang\",\n}"
 },
 {
  "id": "element:roster",
  "kind": "element",
  "name": "roster",
  "evidence": "// OPENING_KIND_META\nroster: {\n    label: \"Roster / breeze block\",\n    type: \"window\",\n    purpose: \"ventilation\",\n    operation: \"perforated\",\n    defaultWidthM: 1.2,\n    defaultHeightM: 1.8,\n    defaultSillHeightM: 0.6,\n    frameMaterial: \"concrete\",\n    privacyLevel: \"high\",\n    shading: \"none\",\n}"
 },
 {
  "id": "element:krawangan",
  "kind": "element",
  "name": "krawangan",
  "evidence": "// OPENING_KIND_META\nkrawangan: {\n    label: \"Krawangan\",\n    type: \"window\",\n    purpose: \"ventilation\",\n    operation: \"perforated\",\n    defaultWidthM: 1.2,\n    defaultHeightM: 1.8,\n    defaultSillHeightM: 0.6,\n    frameMaterial: \"grc\",\n    privacyLevel: \"high\",\n    shading: \"none\",\n}"
 },
 {
  "id": "element:hinged_door",
  "kind": "element",
  "name": "hinged_door",
  "evidence": "// OPENING_KIND_META\nhinged_door: {\n    label: \"Pintu ayun\",\n    type: \"door\",\n    purpose: \"access\",\n    operation: \"swing\",\n    defaultWidthM: 0.9,\n    defaultHeightM: 2.1,\n    defaultSillHeightM: 0,\n    frameMaterial: \"wood\",\n    privacyLevel: \"high\",\n    shading: \"none\",\n}"
 },
 {
  "id": "element:sliding_glass_door",
  "kind": "element",
  "name": "sliding_glass_door",
  "evidence": "// OPENING_KIND_META\nsliding_glass_door: {\n    label: \"Sliding glass door\",\n    type: \"door\",\n    purpose: \"access\",\n    operation: \"sliding\",\n    defaultWidthM: 1.8,\n    defaultHeightM: 2.2,\n    defaultSillHeightM: 0,\n    frameMaterial: \"aluminium\",\n    privacyLevel: \"low\",\n    shading: \"overhang\",\n}"
 },
 {
  "id": "element:pocket_door",
  "kind": "element",
  "name": "pocket_door",
  "evidence": "// OPENING_KIND_META\npocket_door: {\n    label: \"Pocket door\",\n    type: \"door\",\n    purpose: \"access\",\n    operation: \"sliding\",\n    defaultWidthM: 0.9,\n    defaultHeightM: 2.1,\n    defaultSillHeightM: 0,\n    frameMaterial: \"wood\",\n    privacyLevel: \"medium\",\n    shading: \"none\",\n}"
 },
 {
  "id": "element:folding_door",
  "kind": "element",
  "name": "folding_door",
  "evidence": "// OPENING_KIND_META\nfolding_door: {\n    label: \"Folding / bi-fold door\",\n    type: \"door\",\n    purpose: \"access\",\n    operation: \"folding\",\n    defaultWidthM: 2.4,\n    defaultHeightM: 2.2,\n    defaultSillHeightM: 0,\n    frameMaterial: \"aluminium\",\n    privacyLevel: \"low\",\n    shading: \"overhang\",\n}"
 },
 {
  "id": "element:pivot_door",
  "kind": "element",
  "name": "pivot_door",
  "evidence": "// OPENING_KIND_META\npivot_door: {\n    label: \"Pivot door\",\n    type: \"door\",\n    purpose: \"access\",\n    operation: \"pivot\",\n    defaultWidthM: 1.2,\n    defaultHeightM: 2.4,\n    defaultSillHeightM: 0,\n    frameMaterial: \"wood\",\n    privacyLevel: \"high\",\n    shading: \"none\",\n}"
 },
 {
  "id": "element:garage_door",
  "kind": "element",
  "name": "garage_door",
  "evidence": "// OPENING_KIND_META\ngarage_door: {\n    label: \"Pintu garasi (sectional)\",\n    type: \"door\",\n    purpose: \"access\",\n    operation: \"sliding\",\n    defaultWidthM: 2.7,\n    defaultHeightM: 2.2,\n    defaultSillHeightM: 0,\n    frameMaterial: \"steel\",\n    privacyLevel: \"high\",\n    shading: \"none\",\n}"
 },
 {
  "id": "element:open_passage",
  "kind": "element",
  "name": "open_passage",
  "evidence": "// OPENING_KIND_META\nopen_passage: {\n    label: \"Bukaan tanpa pintu\",\n    type: \"door\",\n    purpose: \"access\",\n    operation: \"fixed\",\n    defaultWidthM: 1.2,\n    defaultHeightM: 2.1,\n    defaultSillHeightM: 0,\n    frameMaterial: \"frameless\",\n    privacyLevel: \"low\",\n    shading: \"none\",\n}"
 },
 {
  "id": "element:facade_cutout",
  "kind": "element",
  "name": "facade_cutout",
  "evidence": "// OPENING_KIND_META\nfacade_cutout: {\n    label: \"Void fasad / facade cut-out\",\n    type: \"window\",\n    purpose: \"facade\",\n    operation: \"fixed\",\n    defaultWidthM: 2,\n    defaultHeightM: 2,\n    defaultSillHeightM: 0.6,\n    frameMaterial: \"concrete\",\n    privacyLevel: \"medium\",\n    shading: \"secondary_skin\",\n}"
 },
 {
  "id": "element:cantilever_opening",
  "kind": "element",
  "name": "cantilever_opening",
  "evidence": "// OPENING_KIND_META\ncantilever_opening: {\n    label: \"Cantilever opening\",\n    type: \"window\",\n    purpose: \"facade\",\n    operation: \"fixed\",\n    defaultWidthM: 1.8,\n    defaultHeightM: 1.2,\n    defaultSillHeightM: 0.8,\n    frameMaterial: \"steel\",\n    privacyLevel: \"medium\",\n    shading: \"vertical_fin\",\n}"
 },
 {
  "id": "mechanic:rooftop-hapus-data",
  "name": "Menonaktifkan rooftop menghapus seluruh isi lantainya",
  "evidence": "// editor-store.ts — setRooftop(false)\nsetRooftop: (enabled) => {\n      const { layout } = get();\n      if (!layout) return;\n      const has = layout.floors.some((f) => f.id === \"floor-rooftop\");\n      if (enabled === has) return;\n      commit((l) => {\n        if (enabled) {\n          l.floors.push({\n            id: \"floor-rooftop\",\n            level: l.floors.length + 1,\n            name: \"Rooftop\",\n            heightM: 0.3, // slab tipis — rooftop = dak terbuka (paritas generator)\n          });\n        } else {\n          l.floors = l.floors.filter((f) => f.id !== \"floor-rooftop\");\n          l.rooms = l.rooms.filter((r) => r.floorId !== \"floor-rooftop\");\n          l.openings = l.openings.filter((o) => o.floorId !== \"floor-rooftop\");\n          if (l.roofZones) {\n            l.roofZones = l.roofZones.filter((z) => z.floorId !== \"floor-rooftop\");\n            if (l.roofZones.length === 0) delete l.roofZones;\n          }\n        ",
  "kind": "mechanic"
 },
 {
  "id": "mechanic:removefloor-hapus-data",
  "name": "Menghapus lantai membuang semua elemen di lantai itu",
  "evidence": "// editor-store.ts — removeFloor\nremoveFloor: (floorId) => {\n      const { layout } = get();\n      if (!layout || layout.floors.length <= 1) return;\n      commit((l) => {\n        l.floors = l.floors.filter((f) => f.id !== floorId);\n        l.rooms = l.rooms.filter((r) => r.floorId !== floorId);\n        l.openings = l.openings.filter((o) => o.floorId !== floorId);\n        if (l.exteriorElements) {\n          const next = l.exteriorElements.filter(\n            (el) => el.floorId !== floorId,\n          );\n          if (next.length === 0) delete l.exteriorElements;\n          else l.exteriorElements = next as ExteriorElement[];\n        }\n        if (l.roofZones) {\n          l.roofZones = l.roofZones.filter((z) => z.floorId !== floorId);\n          if (l.roofZones.length === 0) delete l.roofZones;\n        }\n      });\n      set((s) => ({\n        selectedFloorId:\n          s.selectedFloorId === floorId\n            ? (get().layout",
  "kind": "mechanic"
 },
 {
  "id": "mechanic:listrik-default-per-ruang",
  "kind": "mechanic",
  "name": "Titik listrik otomatis mengikuti tipe ruang",
  "evidence": "// constants — jenis titik + default per tipe ruang\nexport const ELECTRICAL_POINT_TYPES: Record<ElectricalPointType, string> = {\n  stopkontak: \"Stopkontak\",\n  stopkontak_daya: \"Stopkontak Daya\",\n  saklar_tunggal: \"Saklar Tunggal\",\n  saklar_ganda: \"Saklar Ganda\",\n  panel: \"Panel Listrik\",\n  data: \"Titik Data/TV\",\n}\n\nexport const ELECTRICAL_DEFAULTS: Record<\n  RoomType,\n  { stopkontak?: number; stopkontak_daya?: number; saklar?: \"tunggal\" | \"ganda\"; data?: number }\n> = {\n  kamar_tidur: { stopkontak: 2, saklar: \"tunggal\" },\n  kamar_mandi: { stopkontak: 1, stopkontak_daya: 1, saklar: \"tunggal\" },\n  ruang_tamu: { stopkontak: 2, saklar: \"tunggal\", data: 1 },\n  ruang_keluarga: { stopkontak: 3, saklar: \"ganda\", data: 1 },\n  dapur: { stopkontak: 3, stopkontak_daya: 1, saklar: \"tunggal\" },\n  ruang_makan: { stopkontak: 2, saklar: \"tunggal\" },\n  musholla: { stopkontak: 1, saklar: \"tunggal\" },\n  laundry: { stopkontak: 2, stopkontak_daya: 1, saklar: \"tunggal\" },\n  gudang: { stopkontak: 1, saklar: \"tunggal\" },\n  balkon: { stopkontak: 1, saklar: \"tunggal\" },\n  rooftop_lounge: { stopkontak: 2, saklar: \"tunggal\", data: 1 },\n  area_kumpul: { stopkontak: 2, saklar: \"tunggal\" },\n  kolam: { stopkontak_daya: 1, saklar: \"tunggal\" },\n  taman: { stopkontak: 1, saklar: \"tunggal\" },\n  workspace: { stopkontak: 2, saklar: \"tunggal\", data: 1 },\n  carport: { stopkontak: 1, saklar: \"tunggal\" },\n  tangga: { saklar: \"ganda\" },\n  void: {},\n}"
 },
 {
  "id": "mechanic:air-default-per-ruang",
  "kind": "mechanic",
  "name": "Titik air otomatis mengikuti tipe ruang",
  "evidence": "// constants — jenis titik + default per tipe ruang\nexport const WATER_POINT_TYPES: Record<WaterPointType, string> = {\n  kloset: \"Kloset\",\n  wastafel: \"Wastafel\",\n  shower: \"Shower\",\n  kran: \"Kran\",\n  kran_taman: \"Kran Taman\",\n  floor_drain: \"Floor Drain\",\n  sink_dapur: \"Sink Dapur\",\n  kran_wudhu: \"Kran Wudhu\",\n}\n\nexport const WATER_DEFAULTS: Record<RoomType, WaterPointType[]> = {\n  kamar_tidur: [],\n  kamar_mandi: [\"kloset\", \"wastafel\", \"shower\", \"floor_drain\"],\n  ruang_tamu: [],\n  ruang_keluarga: [],\n  dapur: [\"sink_dapur\", \"floor_drain\"],\n  ruang_makan: [],\n  musholla: [\"kran_wudhu\"],\n  laundry: [\"kran\", \"floor_drain\"],\n  gudang: [],\n  balkon: [],\n  rooftop_lounge: [],\n  area_kumpul: [],\n  kolam: [],\n  taman: [\"kran_taman\"],\n  workspace: [],\n  carport: [],\n  void: [],\n  tangga: [],\n}"
 },
 {
  "id": "mechanic:kolam-sirkulasi",
  "kind": "mechanic",
  "name": "Sistem sirkulasi kolam dihitung otomatis dari ukuran & kedalaman",
  "evidence": "// pool-circulation.ts — debit, pompa, filter, skimmer semua turunan luas×kedalaman\nexport function poolCirculation(\r\n  room: Pick<Room, \"width\" | \"depth\" | \"areaM2\" | \"poolKind\" | \"poolDepthM\" | \"poolShallowM\" | \"poolDeepM\" | \"poolCirculationType\">\r\n): PoolCirculation {\r\n  const kind: PoolKind = room.poolKind ?? \"renang\"\r\n  const areaM2 = room.areaM2 > 0 ? room.areaM2 : Math.max(0, room.width * room.depth)\r\n  const avgDepthM = effectivePoolDepthRange(room).avgM\r\n  const volumeM3 = round2(areaM2 * avgDepthM)\r\n  const turnoverHours = TURNOVER_H[kind]\r\n  const flowM3h = round1(volumeM3 / turnoverHours)\r\n\r\n  const pumpHp = ceilHalf(flowM3h / 15)\r\n\r\n  // Filter pasir: diameter tabung dari debit (kapasitas ~ luas media).\r\n  const filterDiaInch = flowM3h <= 10 ? 18 : flowM3h <= 18 ? 24 : flowM3h <= 30 ? 30 : 36\r\n  // Spa (dan plunge kecil) memakai filter cartridge; selain itu pasir.\r\n  const filterKind: PoolFilterKind =\r\n    kind === \"spa\" || (kind === \"plunge\" && volumeM3 < 15) ? \"cartridge\" : \"pasir\"\r\n\r\n  const skimmers = Math.max(1, Math.round(areaM2 / 25))\r\n  const returns = Math.max(2, Math.round(areaM2 / 15))\r\n  const mainDrains = areaM2 > 40 ? 2 : 1\r\n\r\n  const suct"
 },
 {
  "id": "mechanic:kolam-listrik",
  "kind": "mechanic",
  "name": "Beban listrik kolam dihitung otomatis (pompa/lampu/heater/klorinator)",
  "evidence": "// pool-electrical.ts\nexport function poolElectrical(\n  room: Pick<\n    Room,\n    | \"width\" | \"depth\" | \"areaM2\" | \"poolKind\" | \"poolDepthM\"\n    | \"poolShallowM\" | \"poolDeepM\" | \"poolCirculationType\"\n    | \"poolHasJets\" | \"poolHeater\" | \"poolSaltChlorinator\"\n  >\n): PoolElectrical {\n  const c = poolCirculation(room)\n  const pumpKw = round2(c.pumpHp * 0.746)\n  const pumpAmps = (pumpKw * 1000) / (220 * 0.8)\n  const pumpBreakerA = roundUpTo(MCB_SIZES, pumpAmps * 1.25)\n\n  const areaM2 = room.areaM2 > 0 ? room.areaM2 : Math.max(0, room.width * room.depth)\n  const lights = Math.max(1, Math.round(areaM2 / 18))\n  const lightsTotalW = lights * POOL_LIGHT_WATT\n  const transformerVa = roundUpTo(TRAFO_SIZES, lightsTotalW * 1.25)\n\n  const jetBlowerW = room.poolHasJets ? POOL_JET_BLOWER_W : 0\n  const heaterW = room.poolHeater ? POOL_HEATER_W : 0\n  const chlorinatorW = room.poolSaltChlorinator ? POOL_CHLORINATOR_W : 0\n\n  con"
 },
 {
  "id": "mechanic:eksterior-keluarga-geometri",
  "kind": "mechanic",
  "name": "Elemen eksterior punya 6 keluarga geometri dengan parameter berbeda",
  "evidence": "// apply.ts — addExteriorElement bercabang per kind\na.type === \"addExteriorElement\") {\r\n        const draft = a.element\r\n        let element: ExteriorElement\r\n        if (\"start\" in draft) {\r\n          element = makeSegmentElement(draft.kind, draft.start, draft.end, {\r\n            floorId: draft.floorId,\r\n            label: draft.label,\r\n            heightM: draft.heightM,\r\n            thicknessM: draft.thicknessM,\r\n            material: draft.material,\r\n          })\r\n        } else if (draft.kind === \"portal_frame\") {\r\n          element = makeFrameElement(draft.x, draft.y, {\r\n            floorId: draft.floorId,\r\n            label: draft.label,\r\n            widthM: draft.widthM,\r\n            heightM: draft.heightM,\r\n            depthM: draft.depthM,\r\n            memberSizeM: draft.memberSizeM,\r\n            rotationDeg: draft.rotationDeg,\r\n            material: draft.material,\r\n          })\r\n        } else if (draft.kind === \"exterior_stair\") {\r\n          element = makeStairElement(draft.x, draft.y, {\r\n            floorId: draft.floorId,\r\n            label: draft.label,\r\n            widthM: draft.widthM,\r\n            lengthM: draft.lengthM,\r\n            riseM: draft.riseM,\r\n            direction: draft.direction,\r\n            material: draft.material,\r\n          })\r\n        } else if (\"points\" in draft) {\r\n          element = makeSurfaceElement(draft.kind, draft.points, {\r\n            floorId: draft.floorId,\r\n            label: draft.label,\r\n            thicknessM: draft.thicknessM,\r\n            material: draft.material,\r\n            scatterSeed: draft.scatterSeed,\r\n          })\r\n        } else if (\r\n          draft.kind === \"asset\" ||\r\n          draft.kind === \"plant\" ||\r\n          draft.kind === \"tree\" ||\r\n          draft.kind === \"exterior_decor\" ||\r\n          draft.kind === \"vehicle\"\r\n        ) {\r\n          element = makeAssetElement(draft.kind, draft.x, draft.y, {\r\n            modelUrl: draft.modelUrl,\r\n            modelAssetId: null,\r\n            fitMode: \"fit_envelope\",\r\n          }, {\r\n            floorId: draft.floorId,\r\n            label: draft.label,\r\n            widthM: draft.widthM,\r\n            depthM: draft.depthM,\r\n            heightM: draft.heightM,\r\n            zM: draft.zM,\r\n            rotation"
 },
 {
  "id": "element:exterior-boundary_wall",
  "kind": "element",
  "name": "boundary_wall",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nboundary_wall: \"Tembok batas\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): segment (start/end)\n// dipetakan ke kategori library aset saat user minta model 3D\nexport const EXTERIOR_KIND_ASSET_CATEGORY: Partial<Record<ExteriorElementKind, string>> = {\n  sliding_gate: \"gate\",\n  swing_gate: \"gate\",\n  pedestrian_gate: \"gate\",\n  boundary_wall: \"fence\",\n  fence: \"fence\",\n  portal_frame: \"arch_element\",\n  canopy: \"arch_element\",\n  column: \"arch_element\",\n  beam: \"arch_element\",\n  facade_panel: \"facade\",\n  exterior_stair: \"arch_element\",\n  planter: \"decor\",\n  exterior_decor: \"decor\",\n}"
 },
 {
  "id": "element:exterior-fence",
  "kind": "element",
  "name": "fence",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nfence: \"Pagar\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): segment (start/end)\n// dipetakan ke kategori library aset saat user minta model 3D\nexport const EXTERIOR_KIND_ASSET_CATEGORY: Partial<Record<ExteriorElementKind, string>> = {\n  sliding_gate: \"gate\",\n  swing_gate: \"gate\",\n  pedestrian_gate: \"gate\",\n  boundary_wall: \"fence\",\n  fence: \"fence\",\n  portal_frame: \"arch_element\",\n  canopy: \"arch_element\",\n  column: \"arch_element\",\n  beam: \"arch_element\",\n  facade_panel: \"facade\",\n  exterior_stair: \"arch_element\",\n  planter: \"decor\",\n  exterior_decor: \"decor\",\n}"
 },
 {
  "id": "element:exterior-sliding_gate",
  "kind": "element",
  "name": "sliding_gate",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nsliding_gate: \"Pintu gerbang geser\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): box (x/y/widthM/depthM/heightM)\n// dipetakan ke kategori library aset saat user minta model 3D\nexport const EXTERIOR_KIND_ASSET_CATEGORY: Partial<Record<ExteriorElementKind, string>> = {\n  sliding_gate: \"gate\",\n  swing_gate: \"gate\",\n  pedestrian_gate: \"gate\",\n  boundary_wall: \"fence\",\n  fence: \"fence\",\n  portal_frame: \"arch_element\",\n  canopy: \"arch_element\",\n  column: \"arch_element\",\n  beam: \"arch_element\",\n  facade_panel: \"facade\",\n  exterior_stair: \"arch_element\",\n  planter: \"decor\",\n  exterior_decor: \"decor\",\n}"
 },
 {
  "id": "element:exterior-swing_gate",
  "kind": "element",
  "name": "swing_gate",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nswing_gate: \"Pintu gerbang ayun\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): box (x/y/widthM/depthM/heightM)\n// dipetakan ke kategori library aset saat user minta model 3D\nexport const EXTERIOR_KIND_ASSET_CATEGORY: Partial<Record<ExteriorElementKind, string>> = {\n  sliding_gate: \"gate\",\n  swing_gate: \"gate\",\n  pedestrian_gate: \"gate\",\n  boundary_wall: \"fence\",\n  fence: \"fence\",\n  portal_frame: \"arch_element\",\n  canopy: \"arch_element\",\n  column: \"arch_element\",\n  beam: \"arch_element\",\n  facade_panel: \"facade\",\n  exterior_stair: \"arch_element\",\n  planter: \"decor\",\n  exterior_decor: \"decor\",\n}"
 },
 {
  "id": "element:exterior-pedestrian_gate",
  "kind": "element",
  "name": "pedestrian_gate",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\npedestrian_gate: \"Pintu pejalan kaki\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): box (x/y/widthM/depthM/heightM)\n// dipetakan ke kategori library aset saat user minta model 3D\nexport const EXTERIOR_KIND_ASSET_CATEGORY: Partial<Record<ExteriorElementKind, string>> = {\n  sliding_gate: \"gate\",\n  swing_gate: \"gate\",\n  pedestrian_gate: \"gate\",\n  boundary_wall: \"fence\",\n  fence: \"fence\",\n  portal_frame: \"arch_element\",\n  canopy: \"arch_element\",\n  column: \"arch_element\",\n  beam: \"arch_element\",\n  facade_panel: \"facade\",\n  exterior_stair: \"arch_element\",\n  planter: \"decor\",\n  exterior_decor: \"decor\",\n}"
 },
 {
  "id": "element:exterior-solid_wall",
  "kind": "element",
  "name": "solid_wall",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nsolid_wall: \"Dinding bebas\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): segment (start/end)\n"
 },
 {
  "id": "element:exterior-facade_panel",
  "kind": "element",
  "name": "facade_panel",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nfacade_panel: \"Panel fasad\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): segment (start/end)\n// dipetakan ke kategori library aset saat user minta model 3D\nexport const EXTERIOR_KIND_ASSET_CATEGORY: Partial<Record<ExteriorElementKind, string>> = {\n  sliding_gate: \"gate\",\n  swing_gate: \"gate\",\n  pedestrian_gate: \"gate\",\n  boundary_wall: \"fence\",\n  fence: \"fence\",\n  portal_frame: \"arch_element\",\n  canopy: \"arch_element\",\n  column: \"arch_element\",\n  beam: \"arch_element\",\n  facade_panel: \"facade\",\n  exterior_stair: \"arch_element\",\n  planter: \"decor\",\n  exterior_decor: \"decor\",\n}"
 },
 {
  "id": "element:exterior-column",
  "kind": "element",
  "name": "column",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\ncolumn: \"Kolom aksen\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): box (x/y/widthM/depthM/heightM)\n// dipetakan ke kategori library aset saat user minta model 3D\nexport const EXTERIOR_KIND_ASSET_CATEGORY: Partial<Record<ExteriorElementKind, string>> = {\n  sliding_gate: \"gate\",\n  swing_gate: \"gate\",\n  pedestrian_gate: \"gate\",\n  boundary_wall: \"fence\",\n  fence: \"fence\",\n  portal_frame: \"arch_element\",\n  canopy: \"arch_element\",\n  column: \"arch_element\",\n  beam: \"arch_element\",\n  facade_panel: \"facade\",\n  exterior_stair: \"arch_element\",\n  planter: \"decor\",\n  exterior_decor: \"decor\",\n}"
 },
 {
  "id": "element:exterior-beam",
  "kind": "element",
  "name": "beam",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nbeam: \"Balok aksen\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): box (x/y/widthM/depthM/heightM)\n// dipetakan ke kategori library aset saat user minta model 3D\nexport const EXTERIOR_KIND_ASSET_CATEGORY: Partial<Record<ExteriorElementKind, string>> = {\n  sliding_gate: \"gate\",\n  swing_gate: \"gate\",\n  pedestrian_gate: \"gate\",\n  boundary_wall: \"fence\",\n  fence: \"fence\",\n  portal_frame: \"arch_element\",\n  canopy: \"arch_element\",\n  column: \"arch_element\",\n  beam: \"arch_element\",\n  facade_panel: \"facade\",\n  exterior_stair: \"arch_element\",\n  planter: \"decor\",\n  exterior_decor: \"decor\",\n}"
 },
 {
  "id": "element:exterior-slab",
  "kind": "element",
  "name": "slab",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nslab: \"Slab bebas\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): box (x/y/widthM/depthM/heightM)\n"
 },
 {
  "id": "element:exterior-canopy",
  "kind": "element",
  "name": "canopy",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\ncanopy: \"Kanopi\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): box (x/y/widthM/depthM/heightM)\n// dipetakan ke kategori library aset saat user minta model 3D\nexport const EXTERIOR_KIND_ASSET_CATEGORY: Partial<Record<ExteriorElementKind, string>> = {\n  sliding_gate: \"gate\",\n  swing_gate: \"gate\",\n  pedestrian_gate: \"gate\",\n  boundary_wall: \"fence\",\n  fence: \"fence\",\n  portal_frame: \"arch_element\",\n  canopy: \"arch_element\",\n  column: \"arch_element\",\n  beam: \"arch_element\",\n  facade_panel: \"facade\",\n  exterior_stair: \"arch_element\",\n  planter: \"decor\",\n  exterior_decor: \"decor\",\n}"
 },
 {
  "id": "element:exterior-planter",
  "kind": "element",
  "name": "planter",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nplanter: \"Planter\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): box (x/y/widthM/depthM/heightM)\n// dipetakan ke kategori library aset saat user minta model 3D\nexport const EXTERIOR_KIND_ASSET_CATEGORY: Partial<Record<ExteriorElementKind, string>> = {\n  sliding_gate: \"gate\",\n  swing_gate: \"gate\",\n  pedestrian_gate: \"gate\",\n  boundary_wall: \"fence\",\n  fence: \"fence\",\n  portal_frame: \"arch_element\",\n  canopy: \"arch_element\",\n  column: \"arch_element\",\n  beam: \"arch_element\",\n  facade_panel: \"facade\",\n  exterior_stair: \"arch_element\",\n  planter: \"decor\",\n  exterior_decor: \"decor\",\n}"
 },
 {
  "id": "element:exterior-portal_frame",
  "kind": "element",
  "name": "portal_frame",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nportal_frame: \"Portal\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): portal_frame (x/y/widthM/heightM/depthM)\n// dipetakan ke kategori library aset saat user minta model 3D\nexport const EXTERIOR_KIND_ASSET_CATEGORY: Partial<Record<ExteriorElementKind, string>> = {\n  sliding_gate: \"gate\",\n  swing_gate: \"gate\",\n  pedestrian_gate: \"gate\",\n  boundary_wall: \"fence\",\n  fence: \"fence\",\n  portal_frame: \"arch_element\",\n  canopy: \"arch_element\",\n  column: \"arch_element\",\n  beam: \"arch_element\",\n  facade_panel: \"facade\",\n  exterior_stair: \"arch_element\",\n  planter: \"decor\",\n  exterior_decor: \"decor\",\n}"
 },
 {
  "id": "element:exterior-exterior_stair",
  "kind": "element",
  "name": "exterior_stair",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nexterior_stair: \"Tangga luar\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): exterior_stair (x/y/widthM/lengthM/riseM)\n// dipetakan ke kategori library aset saat user minta model 3D\nexport const EXTERIOR_KIND_ASSET_CATEGORY: Partial<Record<ExteriorElementKind, string>> = {\n  sliding_gate: \"gate\",\n  swing_gate: \"gate\",\n  pedestrian_gate: \"gate\",\n  boundary_wall: \"fence\",\n  fence: \"fence\",\n  portal_frame: \"arch_element\",\n  canopy: \"arch_element\",\n  column: \"arch_element\",\n  beam: \"arch_element\",\n  facade_panel: \"facade\",\n  exterior_stair: \"arch_element\",\n  planter: \"decor\",\n  exterior_decor: \"decor\",\n}"
 },
 {
  "id": "element:exterior-driveway",
  "kind": "element",
  "name": "driveway",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\ndriveway: \"Driveway\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): surface (points[])\n"
 },
 {
  "id": "element:exterior-walkway",
  "kind": "element",
  "name": "walkway",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nwalkway: \"Walkway\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): surface (points[])\n"
 },
 {
  "id": "element:exterior-terrace_surface",
  "kind": "element",
  "name": "terrace_surface",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nterrace_surface: \"Teras\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): surface (points[])\n"
 },
 {
  "id": "element:exterior-garden_bed",
  "kind": "element",
  "name": "garden_bed",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\ngarden_bed: \"Taman / planting bed\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): surface (points[])\n"
 },
 {
  "id": "element:exterior-asset",
  "kind": "element",
  "name": "asset",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nasset: \"Aset eksterior\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): asset (x/y + modelUrl)\n"
 },
 {
  "id": "element:exterior-plant",
  "kind": "element",
  "name": "plant",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nplant: \"Tanaman\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): asset (x/y + modelUrl)\n"
 },
 {
  "id": "element:exterior-tree",
  "kind": "element",
  "name": "tree",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\ntree: \"Pohon\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): asset (x/y + modelUrl)\n"
 },
 {
  "id": "element:exterior-exterior_decor",
  "kind": "element",
  "name": "exterior_decor",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nexterior_decor: \"Dekor eksterior\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): asset (x/y + modelUrl)\n// dipetakan ke kategori library aset saat user minta model 3D\nexport const EXTERIOR_KIND_ASSET_CATEGORY: Partial<Record<ExteriorElementKind, string>> = {\n  sliding_gate: \"gate\",\n  swing_gate: \"gate\",\n  pedestrian_gate: \"gate\",\n  boundary_wall: \"fence\",\n  fence: \"fence\",\n  portal_frame: \"arch_element\",\n  canopy: \"arch_element\",\n  column: \"arch_element\",\n  beam: \"arch_element\",\n  facade_panel: \"facade\",\n  exterior_stair: \"arch_element\",\n  planter: \"decor\",\n  exterior_decor: \"decor\",\n}"
 },
 {
  "id": "element:exterior-vehicle",
  "kind": "element",
  "name": "vehicle",
  "evidence": "// EDITOR_EXTERIOR_KIND_LABELS\nvehicle: \"Kendaraan\"\n// keluarga geometri (dispatch apply.ts addExteriorElement): asset (x/y + modelUrl)\n"
 },
 {
  "id": "mechanic:roofzones-vs-legacy",
  "kind": "mechanic",
  "name": "Atap eksplisit (roofZones) menang atas layout.roof lama bila keduanya ada",
  "evidence": "// types/exterior.ts — bentuk RoofZone\nexport type RoofZone = {\n  id: string;\n  floorId?: string;\n  type: \"datar\" | \"pelana\" | \"limasan\" | \"miring\";\n  x: number;\n  y: number;\n  widthM: number;\n  depthM: number;\n  slopeDeg: number;\n  overhangM: number;\n  materialId?: string;\n  lowSide?: \"n\" | \"s\" | \"w\" | \"e\";\n};\n\n// build-model.ts — komentar: roofZones eksplisit; absent = fallback ke layout.roof\nMaterial atap eksplisit per prim (roofZones); absent = layout.roof fallback. */\n  roofMaterial?: RoofMaterial\n  opening?: Pick<\n    Opening,\n    | \"type\"\n    | \"kind\"\n    | \"purpose\"\n    | \"operation\"\n    | \"frameMaterial\"\n    | \"frameColor\"\n    | \"privacyLevel\"\n    | \"shading\"\n    | \"sillHeightM\"\n    | \"headHeightM\"\n    | \"notes\"\n    | \"modelUrl\"\n    | \"curtainModelUrl\"\n  >\n  /**\n   * Untuk prim louver yang mewakili satu elemen fasad kustom. Renderer memakai\n   * envelope `args` untuk fit model\n\n// material atap yang sah + harga\nexport const ROOF_PRICES: Record<RoofMaterial, number> = {\n  genteng_beton: 400_000,\n  genteng_keramik: 500_000,\n  metal: 350_000,\n  aspal: 550_000,\n}"
 },
 {
  "id": "mechanic:fasad-cladding-vs-template",
  "kind": "mechanic",
  "name": "setWallCladding mengubah satu dinding; applyFacadeTemplate mengubah semuanya sekaligus",
  "evidence": "// facade-claddings.ts — daftar id cladding yang sah\nbatu_alam_gelap, batu_andesit, marmer_krem, marmer_carrara, granit_hitam, beton_ekspos, kayu_cladding, kayu_alder, kayu_terang, bata_ekspos, bata_putih\n\n// skema aksi\nz.object({\r\n    type: z.literal(\"setWallCladding\"),\r\n    wallId: z.string().min(1),\r\n    claddingId: facadeCladdingIdSchema.nullable(),\r\n    face: facadeFaceSchema.optional(),\r\n  }),\nz.object({ type: z.literal(\"applyFacadeTemplate\"), templateId: z.enum(FACADE_COMPOSER_TEMPLATE_IDS) }),"
 }
]